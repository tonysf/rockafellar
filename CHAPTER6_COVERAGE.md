# Chapter 6 Coverage Ledger

This ledger tracks Chapter 6 of Rockafellar--Wets, *Variational Geometry*,
result by result.  `Exact` means that every substantive mathematical clause is
available under the project's stated finite-dimensional real-space
assumptions.  `Adapted` records a mathematically necessary change to a printed
statement, together with a formal explanation or counterexample.  `Partial`
records a sound API for only part of the printed result, with the remaining
clauses named.  `Missing` means that no result-specific API has yet been
integrated.

Formalization covers Sections A through C in full and selected results from
Sections D through I: every numbered result from 6.1 through 6.13 is exact,
as are 6.16--6.18, 6.20--6.26, 6.33, 6.41, and 6.45; 6.34 and 6.49 are
adapted, with the necessary corrections recorded in their rows; and 6.29
carries a sound API for one of its implications.  The supporting modules
include canonical locality and elementary-cone formulas, continuous linear
right inverses, local sections of submersions, scalar `C¹` strict-majorant
infrastructure, binary and finite Euclidean-product cone formulas, and the
finite-box and nonnegative-orthant layer.

Exercise 6.7 is now complete.
[`RegularNormalChangeOfCoordinates.lean`](RockafellarWets/Chapter6/RegularNormalChangeOfCoordinates.lean)
uses `WithLp 2 (F × ker ∇F(x̄))` for the augmented codomain, resolving the
sup-norm obstruction that kept the adjoint from the earlier proof, and
[`LimitingNormalChangeOfCoordinates.lean`](RockafellarWets/Chapter6/LimitingNormalChangeOfCoordinates.lean)
proves the limiting-normal formula by lifting nearby regular-normal witnesses
through the local sections of
[`LocalSubmersion.lean`](RockafellarWets/Chapter6/LocalSubmersion.lean) and
controlling the varying adjoints with the continuously varying right inverses
of
[`ContinuousRightInverses.lean`](RockafellarWets/Chapter6/ContinuousRightInverses.lean).
Example 6.8 is then 6.7 with `D = {0}` in
[`SmoothManifolds.lean`](RockafellarWets/Chapter6/SmoothManifolds.lean), with
geometric derivability and local closedness proved rather than read off the
formulas.

The chapter's limits are Painleve--Kuratowski limits indexed by a *scaling
parameter* rather than by an argument, so nothing new is needed to state them:
the filter-native set limits of
[`SetLimitsAlong.lean`](RockafellarWets/Chapter5/SetLimitsAlong.lean), written
for formula 5(1) along an arbitrary index filter, are used here along
`𝓝[>] 0` on `ℝ`.  That is why 6.2 costs only the two identifications and
inherits closedness of both cones from Chapter 5.  Section B is the same story
one level up: the general normal cone of 6.3 *is* the relative outer limit
`svOuterLimitWithin (regularNormalCone C) C x̄` of formula 5(1) applied to the
regular normal cone mapping, so 6.5's closedness is
`isClosed_outerSetLimitAlong` and 6.6 is the statement that a relative outer
limit absorbs a second application of itself.

**Ambient setting.**  Section A is stated over
`[NormedAddCommGroup E] [NormedSpace ℝ E]`, since tangency is a statement
about difference quotients and needs no pairing.  Section B needs one, and
takes `[InnerProductSpace ℝ E]`, the setting already used in Chapters 1--3;
a dual-space pairing was rejected because it would fork every normal-cone
statement between `E` and `E →L[ℝ] ℝ` for no gain in a book written in
`IRⁿ`, where the two are identified throughout.  Section A is left exactly as
it was: an inner product space is a normed space, so the tangent cones apply
unchanged.  `[FiniteDimensional ℝ E]` is *not* a standing assumption of the
Section B module; it is asked for by name in the one place the book's proofs
use it, the implication `⇐` of 6(6), where unit-length difference quotients
are made to converge along a subsequence.

**The convention 6(8).**  Formula 6(8) sets `N_C(x̄) = N̂_C(x̄) = ∅` for
`x̄ ∉ C`.  This is carried definitionally, by the conjunct `x̄ ∈ C` in both
definitions, rather than by a case split at each use.  Without it the `ε`
inequality of 6(5) would hold *vacuously* at every `x̄ ∉ cl C`, since
`𝓝[C] x̄` is then the trivial filter, and `N̂_C(x̄)` would come out as the
whole space.  The convention earns its place immediately: it is what makes
`dom N_C = dom N̂_C = C` true, what lets the relative outer limit of 6(7) be
replaced by the full one, and what makes the closed-set graph formula
`gph N_C = cl(gph N̂_C)` come out.

| Status | Count |
| --- | ---: |
| Exact | 26 |
| Adapted | 2 |
| Partial | 1 |
| Missing | 20 |
| **Total** | **49** |

| Result | Status | Lean coverage / remaining work |
|---|---|---|
| 6.1 Tangent vectors and geometric derivability | **Exact** | [`TangentCones.lean`](RockafellarWets/Chapter6/TangentCones.lean) defines the tangent cone by formula 6(2), the derivable tangent vectors by the path condition, and geometric derivability as the inclusion of the first in the second.  The path `ξ : [0, ε] → C` is carried as a total function `ℝ → E` constrained on `[0, ε]`, and its right derivative at `0` as a limit along `𝓝[>] 0`, which is what the book's `ξ'₊(0)` means; **no continuity of `ξ` away from `0` is asked, exactly as printed**, and that is what makes the hard half of 6.2 provable at all.  The convergence `xν →_C x̄` printed alongside `[xν - x̄]/τν → w` in 6(2) is redundant -- the differences are `τν` times a convergent sequence -- so `mem_tangentCone_of_forall` is supplied as a constructor that does not ask for it, and the definition keeps the printed form. |
| 6.2 Tangent cone properties | **Exact** | [`TangentCones.lean`](RockafellarWets/Chapter6/TangentCones.lean) proves every clause.  Formula 6(3) is the identification of Definition 6.1 with `outerSetLimitAlong (𝓝[>] 0)` of the magnified difference sets `τ ↦ τ⁻¹(C - x̄)`, and the derivable vectors with `innerSetLimitAlong` of the same family; closedness of both cones is then `isClosed_outerSetLimitAlong` and `isClosed_innerSetLimitAlong` from [`SetLimitsAlong.lean`](RockafellarWets/Chapter5/SetLimitsAlong.lean), and the concluding clause -- geometric derivability is convergence of `[C - x̄]/τ` as `τ ↓ 0` -- is equality of the two limits.  **The Chapter 5 set limits transfer with no change**: they were written along an arbitrary index filter for formula 5(1), and here the index is the scaling parameter rather than the argument.  Both cones are shown to be cones in the Chapter 3 sense, the zero vector coming from the constant sequence at `x̄` and the rescaling of `w` by `c > 0` from rescaling `τν` by `1/c`, respectively from reparameterizing the path by `t ↦ ct`.  The one substantive direction is that an inner-limit vector is *derivable*, where a path has to be produced: it is chosen one point per `τ`, within `τ` of the best point of `τ⁻¹(C - x̄)` available, so the error is at most `d(w, τ⁻¹(C - x̄)) + τ` and both terms vanish -- the slack device of the pointwise selection remark before 5.57.  **`x̄ ∈ C` is needed only for the derivable half**, since `ξ(0) = x̄` forces it while the inner limit puts `x̄` only in `cl C`; formula 6(3) itself is proved at every `x̄`.  The unnumbered Figure 6-4 example of a set whose tangent cone is a half-plane while its derivable cone is `{0}` is not formalized. |
| 6.3 Normal vectors | **Exact** | [`NormalCones.lean`](RockafellarWets/Chapter6/NormalCones.lean) defines `regularNormalCone` by formula 6(4) and `normalCone` by the sequential clause of 6.3, then proves the identification `normalCone C x̄ = svOuterLimitWithin (regularNormalCone C) C x̄` that formula 6(7) records.  **6(4) is formalized as 6(5)**, in the filter form `∀ ε > 0, ∀ᶠ y in 𝓝[C] x̄, ⟨v, y - x̄⟩ ≤ ε‖y - x̄‖`, with `mem_regularNormalCone_iff` giving the `ε`-`δ` reading: the book's `o(|x - x̄|)` is not a function but a property, and the text states outright that 6(4) *means* 6(5), the two being equivalent because `max{0, ⟨v, x - x̄⟩}` is the `o` term.  The base point `x̄` may be taken in the closure of `C` without harm; excluding `x = x̄` from 6(5), as the book does, changes nothing, both sides of the inequality vanishing there.  The convergence `xν →_C x̄` printed in the sequential clause asks `xν ∈ C` separately, but that is already carried by `vν ∈ N̂_C(xν)` under the convention 6(8), so `mem_normalCone_of_forall` is supplied as a constructor that does not ask for it, as `mem_tangentCone_of_forall` is in 6.1.  The identification is proved directly in both directions rather than through the Chapter 5 sequential bridge, one point `(xν, vν)` chosen per `ν` within `1/(ν+1)` of `x̄` and of `v`; the bridge would have produced a subsequence to discard afterwards. |
| 6.4 Clarke regularity of sets | **Exact** | [`NormalCones.lean`](RockafellarWets/Chapter6/NormalCones.lean) defines `IsClarkeRegularAt` as printed, the conjunction of local closedness at `x̄` with `N_C(x̄) = N̂_C(x̄)`, and `isClarkeRegularAt_iff` records that only the inclusion `N_C(x̄) ⊆ N̂_C(x̄)` is at issue, the reverse being 6(7).  Local closedness at a point is the book's notion from the paragraph opening Chapter 1I* -- `C ∩ V` closed for some closed neighborhood `V` of `x̄`, which need not meet `C` -- and had no definition in the repository, so `IsLocallyClosedAt` is introduced here.  It is deliberately *not* Mathlib's `IsLocallyClosed`, which asks a set to be the intersection of an open set with a closed one and is a different notion; `IsClosed.isLocallyClosedAt` records the book's remark that a closed set is locally closed everywhere. |
| 6.5 Normal cone properties | **Exact** | [`NormalCones.lean`](RockafellarWets/Chapter6/NormalCones.lean) proves every clause: both sets are cones, `N_C(x̄)` is closed, `N̂_C(x̄)` is closed and convex, formula 6(6) characterizes `N̂_C(x̄)` against the tangent cone, and formula 6(7) is the identification proved at 6.3, with `N̂_C(x̄) ⊆ N_C(x̄)` from the constant sequence.  Closedness of `N_C(x̄)` is `isClosed_outerSetLimitAlong` through 6(7), exactly as the book has it.  **Convexity and closedness of `N̂_C(x̄)` are proved directly from 6(5), not through 6(6) as the book routes them**: a convex combination of two vectors satisfying the `ε` inequality satisfies it with the same `ε`, and a vector within `ε/2` of one satisfying the `ε/2` inequality satisfies the `ε` one by Cauchy--Schwarz.  The point of the detour is that both statements are then dimension-free, whereas the book's route through 6(6) would import compactness of the unit ball into two facts that do not need it.  6(6) itself splits accordingly: `⇒` holds in any inner product space, while `⇐` is stated with `[FiniteDimensional ℝ E]`.  That is not a hypothesis added to the printed statement -- it is the book's ambient `IRⁿ`, and it is used for precisely the step the printed proof takes, normalizing `[xν - x̄]/|xν - x̄|` to unit length and passing to a convergent subsequence.  `regularNormalCone_eq_iInter` records 6(6) in the form the book's convexity argument uses, as an intersection of closed half-spaces.  The figures 6-5 and 6-6, and the accompanying remark that the inward corner point is derivable without being regular, are not formalized. |
| 6.6 Limits of normal vectors | **Exact** | [`NormalCones.lean`](RockafellarWets/Chapter6/NormalCones.lean) proves both readings: `mem_normalCone_of_tendsto` is the printed sequential statement and `svOscWithinAt_normalCone` is the `In other words` clause, outer semicontinuity of `N_C` at `x̄` relative to `C` in the sense of Definition 5.4, with `svOscOn_normalCone` for the whole of `C`.  **The proof is not the book's.**  The book argues that `gph N_C` is the closure of `gph N̂_C` in `C × IRⁿ` and hence closed there; formalized through 6(7), the statement is instead that a relative outer limit absorbs a second application of itself, `svOuterLimitWithin_svOuterLimitWithin_subset`, which is a fact about formula 5(1) in any topological space and needs no product topology, no metric and no sequences.  The book's closure form is then proved separately, as `svGraph_normalCone`, for a closed `C`, together with the other two claims of the paragraph after 6.6: `dom N_C = dom N̂_C = C`, and `N_C(x̄) = lim sup_{x → x̄} N̂_C(x)` along the *full* neighborhood filter.  The last of these is split, since `normalCone_eq_svOuterLimit_of_mem` shows it needs no closedness at points of `C` -- emptiness of `N̂_C` off `C` already forces the relative and full outer limits to agree -- and closedness of `C` is used only to make both sides empty off `C`.  `x̄ ∈ C` is a hypothesis throughout: it is what writing `N_C(x̄)` presupposes in Definition 6.3, and what the book's own `closed relative to C × IRⁿ` says. |
| 6.7 Change of coordinates | **Exact** | All three printed formulas are proved as equalities for `C = G⁻¹(D)`, with no hypothesis on `D`.  [`ChangeOfCoordinates.lean`](RockafellarWets/Chapter6/ChangeOfCoordinates.lean) proves the tangent formula `tangentCone_preimage_of_surjective`, `T_C(x̄) = ∇G(x̄)⁻¹(T_D(ū))`, by the book's Guide: the derivative is made square by pairing `G` with the orthogonal projection onto `ker ∇G(x̄)` -- the book's basis `a₁, …, a_{n−m}` -- and the inverse function theorem reduces to the invertible case `tangentCone_preimage`.  [`RegularNormalChangeOfCoordinates.lean`](RockafellarWets/Chapter6/RegularNormalChangeOfCoordinates.lean) proves the regular-normal formula `regularNormalCone_preimage_of_surjective` the same way, with the augmented codomain placed on `WithLp 2 (F × ker ∇G(x̄))` so that the augmented derivative has the adjoint the sup-normed product lacks; the one-way chain rule `adjoint_image_regularNormalCone_subset_preimage` needs only Fréchet differentiability at the base point.  [`LimitingNormalChangeOfCoordinates.lean`](RockafellarWets/Chapter6/LimitingNormalChangeOfCoordinates.lean) proves the limiting-normal formula `normalCone_preimage_of_surjective`, `N_C(x̄) = ∇G(x̄)* N_D(ū)`, as the inclusions `adjoint_image_normalCone_subset_preimage` and `normalCone_preimage_subset_adjoint_image`.  **Its proof is not the book's coordinate change but a direct limit argument on the witnesses of Definition 6.3.**  For `⊇`, regular normals `vν ∈ N̂_D(yν)` with `yν → ū` are lifted by the local section of [`LocalSubmersion.lean`](RockafellarWets/Chapter6/LocalSubmersion.lean) to `xν → x̄` with `G(xν) = yν` for all large `ν`, and the pointwise chain rule puts `∇G(xν)* vν` in `N̂_C(xν)`; for `⊆`, the regular-normal formula is applied at each `xν → x̄` -- `∇G(xν)` is surjective on a tail because the continuously varying right inverses of [`ContinuousRightInverses.lean`](RockafellarWets/Chapter6/ContinuousRightInverses.lean) are eventually right inverses of `∇G(xν)` -- giving multipliers `vν ∈ N̂_D(G(xν))` with `∇G(xν)* vν = wν`, bounded by `‖vν‖ ≤ ‖Rν‖ ‖wν‖` from `norm_le_rightInverse_norm_mul_adjoint`; a convergent subsequence gives `v ∈ N_D(ū)` by 6.3 and `∇G(x̄)* v = w` by uniqueness of limits, since `∇G(xν) → ∇G(x̄)` and the adjoint is an isometry.  Because the base points move in both directions, **the limiting clause takes `ContDiffAt ℝ 1 G x̄` -- the printed hypothesis, the book's `smooth` meaning `C¹`, localized to a neighborhood of `x̄` -- whereas the tangent and regular-normal clauses need only a strict derivative at `x̄`** and are stated with an explicit `G'` and `HasStrictFDerivAt G G' x̄` rather than `fderiv ℝ G x̄`; a user holding `ContDiffAt ℝ 1 G x̄` obtains those two clauses through `hG.hasStrictFDerivAt one_ne_zero`, which is how [`SmoothManifolds.lean`](RockafellarWets/Chapter6/SmoothManifolds.lean) combines the three.  Full rank `m` is `Function.Surjective (fderiv ℝ G x̄)`.  `x̄ ∈ C` is not asked: under 6(8) both sides are empty when `G(x̄) ∉ D`.  The spaces are real Hilbert spaces, `[CompleteSpace E]` being asked on the domain for the orthogonal projection onto the kernel and for the adjoint, with no `[FiniteDimensional ℝ E]` anywhere; `[FiniteDimensional ℝ F]` is asked only on the *codomain* of the limiting clause, used substantively once, in `isCompact_closedBall`, to extract the convergent subsequence of multipliers -- the step the book takes for granted in `IRᵐ` -- and otherwise only to supply the completeness of `F` that the adjoint and the imported right-inverse and local-section lemmas need; the tangent and regular-normal clauses ask `[CompleteSpace F]` instead.  The easy inclusion `adjoint_image_normalCone_subset_preimage` inherits `[FiniteDimensional ℝ F]` from its section although completeness of `F` would do.  The Guide's reduction to `m = n` is followed only in the tangent and regular-normal proofs. |
| 6.8 Tangents and normals to smooth manifolds | **Exact** | [`SmoothManifolds.lean`](RockafellarWets/Chapter6/SmoothManifolds.lean) proves every clause.  The hypothesis that `C` is `represented relative to an open neighborhood O ∈ N(x̄)` as the solution set of `F(x) = 0` is the germ equality `C =ᶠ[𝓝 x̄] G⁻¹(0)`, taken together with `x̄ ∈ C`, `ContDiffAt ℝ 1 G x̄` and surjectivity of `fderiv ℝ G x̄`, the latter being `∇F(x̄)` of full rank `m`; `smoothManifold_tangents_and_normals_of_isOpen` restates the whole example in the book's own form, an open `O ∋ x̄`, `ContDiffOn ℝ 1 G O` and `C ∩ O = G⁻¹(0) ∩ O`.  The cone formulas `tangentCone_smoothManifold`, `regularNormalCone_smoothManifold` and `normalCone_smoothManifold` are exactly the printed `Detail`: transfer across the germ by the locality theorems of [`ElementaryCones.lean`](RockafellarWets/Chapter6/ElementaryCones.lean), then 6.7 with `D = {0}` -- `tangentCone_preimage_of_surjective`, `regularNormalCone_preimage_of_surjective` and `normalCone_preimage_of_surjective` -- and the singleton cones `{0}` and `univ`.  Clarke regularity is `isClarkeRegularAt_smoothManifold`, the two normal formulas agreeing together with `isLocallyClosedAt_smoothManifold`, which **proves local closedness rather than assuming it** -- on a closed ball inside the germ neighborhood `C` is the zero set of a map continuous there -- and needs neither `x̄ ∈ C`, surjectivity nor finite dimension.  **Geometric derivability does not follow from 6.7**, which says nothing about derivable directions, so `isGeometricallyDerivable_smoothManifold` argues directly: Mathlib's implicit function theorem (`HasStrictFDerivAt.implicitFunction`) parametrizes the level set through `x̄` by `ker ∇G(x̄)` with the kernel inclusion as derivative at `0`, and the straight line `t ↦ tw` in that coordinate maps to a path in `C` on `[0, δ]` whose right derivative at `0` is `w`; `derivableCone_smoothManifold` then gives the derivable cone the same formula.  The orthogonal-complement reading is `tangentCone_eq_ker_smoothManifold`, `normalCone_eq_orthogonal_ker_smoothManifold` and `tangentCone_normalCone_orthogonally_complementary_smoothManifold`, which exhibits submodules `T = ker ∇G(x̄)` and `N = Tᗮ` with `T_C(x̄) = T`, `N_C(x̄) = N`, `T = Nᗮ` and `IsCompl T N`; the identity `range ∇G(x̄)* = (ker ∇G(x̄))ᗮ` behind it is `range_adjoint_eq_orthogonal_ker`, Mathlib's orthogonal-complement description of the range of an adjoint with the closure of the range removed by `[FiniteDimensional ℝ E]`.  The count `d = n − m` is `finrank_tangentCone_smoothManifold`, rank--nullity for the surjective derivative, stated for `ker ∇G(x̄)`, which is `T_C(x̄)` by the tangent formula.  `smoothManifold_tangents_and_normals` bundles all eight conclusions.  `[FiniteDimensional ℝ E]` and `[FiniteDimensional ℝ F]` are the book's `IRⁿ` and `IRᵐ`, and are used for precisely what the printed argument uses them for: completeness for the adjoint and for the inverse-function machinery of 6.7, the finite-dimensional codomain that the implicit function theorem and the limiting-normal formula of 6.7 require, and the closedness of `range ∇G(x̄)*`.  Asking only `ContDiffAt` at `x̄` in the germ form is a mild generalization of the printed `C¹` on all of `O`; no closedness, convexity or regularity of `C` is assumed.  Figure 6-7 and the remark that these cones reduce to the classical tangent and normal spaces of differential geometry are not formalized, and no manifold structure is introduced. |
| 6.9 Tangents and normals to convex sets | **Exact** | [`ConvexSets.lean`](RockafellarWets/Chapter6/ConvexSets.lean) proves every clause.  The set `K` of the printed proof is named `radialCone C x̄`, the directions `w` with `x̄ + λw ∈ C` for some `λ > 0`; it is shown convex for convex `C` and open for open `C`.  Geometric derivability, `T_C(x̄) = cl K` and the same formula for the derivable cone all come from the sandwich `K ⊆ D_C(x̄) ⊆ T_C(x̄) ⊆ cl K`, whose left-hand inclusion is the straight path `t ↦ x̄ + tw` -- admissible because Definition 6.1 asks no continuity of the path away from `0`, and in `C` by convexity -- and whose right-hand inclusion holds for every set, each difference quotient of 6(2) being itself a radial direction.  **The normal cone formula is proved without 6(6)**, and so without finite-dimensionality: `⟨v, x - x̄⟩ ≤ 0` for all `x ∈ C` gives 6(4) with any `ε` at all, while conversely `x - x̄` is a radial direction and hence a tangent vector, so the `⇒` half of 6(6), which is dimension-free, applies.  `N_C(x̄) = N̂_C(x̄)` is then the book's limit argument, `⟨vν, x - xν⟩ ≤ 0` passing to the limit; local closedness plays no part in it and enters only through Definition 6.4, exactly as the concluding sentence of 6.9 says.  The interior formula is the one clause needing `[FiniteDimensional ℝ E]`: when `int C ≠ ∅` it is `cl K = cl K₀` -- from `C ⊆ cl(int C)`, which is 2.33 -- followed by `int(cl K₀) = int K₀ = K₀` for the convex *open* set `K₀`, and when `int C = ∅` it is the statement that `cl K` then has empty interior, which is false in infinite dimensions and here comes from `span(C - x̄)` being closed and from a convex set with `aff C = IRⁿ` having interior points.  Figure 6-8 and the supporting-halfspace remark are not formalized. |
| 6.10 Tangents and normals to boxes | **Exact** | [`FiniteBoxes.lean`](RockafellarWets/Chapter6/FiniteBoxes.lean) proves every clause for an arbitrary finite index type.  The box `finiteBox C = {x | ∀ i, x i ∈ C i}` lives in `EuclideanSpace ℝ ι`, `[Fintype ι]`, so that the coordinate product carries the Euclidean inner product that 6(4) is written against and not the sup norm of `ι → ℝ`, and the coordinate hypothesis is `IsNonemptyClosedInterval (C i)` -- nonempty, closed and order connected -- which is exactly the class `univ`, `Ici a`, `Iic b`, `Icc a b` with `a ≤ b`, the one-point intervals included.  `variationalGeometry_finiteBox_of_closedIntervals` bundles, at every `x̄ ∈ finiteBox C`, closedness and convexity of the box (the printed *Detail*), geometric derivability, Clarke regularity, and the four product *equalities* `tangentCone_finiteBox_of_closedIntervals`, `derivableCone_finiteBox_of_closedIntervals`, `regularNormalCone_finiteBox_of_closedIntervals` and `normalCone_finiteBox_of_closedIntervals`, whose right-hand sides are `{w | ∀ i, w i ∈ intervalTangentDirections (C i) (x̄ i)}` and `{v | ∀ i, v i ∈ intervalNormalDirections (C i) (x̄ i)}`.  **The four printed cases are encoded by the order predicates `IsGreatest (C i) (x̄ i)` and `IsLeast (C i) (x̄ i)` rather than by a case split on the shape of `C i`**: `intervalTangentDirections S x` is `{w | (IsGreatest S x → w ≤ 0) ∧ (IsLeast S x → 0 ≤ w)}` and `intervalNormalDirections` is its mirror with the predicates negated, so both predicates give `{0}` and `univ`, `IsGreatest` alone gives `Iic 0` and `Ici 0`, `IsLeast` alone gives `Ici 0` and `Iic 0`, and neither gives `univ` and `{0}`, in the printed order; `isGreatest_and_isLeast_iff_eq_singleton` identifies the first case with the one-point interval, which is what makes *only the right endpoint* and *only the left endpoint* faithful, and for an order-connected set *neither* is exactly interiority.  The eight case lemmas `intervalTangentDirections_of_isGreatest_of_isLeast`, `_of_isGreatest`, `_of_isLeast`, `_of_not_isGreatest_of_not_isLeast` and their `intervalNormalDirections` counterparts, together with the specializations `_univ`, `_singleton`, `_Ici_self`, `_Iic_self`, `_Icc_left`, `_Icc_right`, `_Icc_interior`, reduce the classifiers to the printed sets; the whole-line case, previously unstated, is checked against `tangentCone_univ` and `normalCone_univ` of [`ElementaryCones.lean`](RockafellarWets/Chapter6/ElementaryCones.lean), and the bounded, ray and singleton formulas of [`IntervalCones.lean`](RockafellarWets/Chapter6/IntervalCones.lean) are recovered from one scalar theorem rather than reused.  **The scalar identities `tangentCone_eq_intervalTangentDirections`, `derivableCone_eq_intervalTangentDirections`, `regularNormalCone_eq_intervalNormalDirections` and `normalCone_eq_intervalNormalDirections` need only `S.OrdConnected` and `x ∈ S`, not closedness**: the radial cone of *any* subset of `ℝ` at one of its points is already the four-case set (`radialCone_eq_intervalTangentDirections`, the scaling `λ = (y − x)/w` landing on a witness `y`), that set is closed, and 6.9 in [`ConvexSets.lean`](RockafellarWets/Chapter6/ConvexSets.lean) turns it into both tangent cones and turns `⟨v, y − x̄⟩ ≤ 0` into the sign conditions of the printed *Detail*.  The product layer is generic and reusable: `derivableCone_finiteBox` and `tangentCone_finiteBox_subset` hold for arbitrary coordinate sets, the finitely many coordinate paths being restricted to a common positive radius by `Finite.exists_min`; `tangentCone_finiteBox` asks the coordinates to be geometrically derivable, and `regularNormalCone_finiteBox`, `normalCone_finiteBox` ask them to be convex, both computed by 6.9 and isolating a coordinate by perturbing `x̄` along `EuclideanSpace.single` -- hypotheses automatic for intervals, the hypothesis-free product formulas being the business of 6.41.  Clarke regularity is `isClarkeRegularAt_of_convex` with `finiteBox_isClosed` supplying local closedness, the one place closedness of the `C i` is used.  **`ι` is any `Fintype`, the empty index type included** (the box is then the whole zero-dimensional space and every product statement reads as one about `univ`), a generalization of the printed `IRⁿ`; finiteness enters only through the norm and finite inner-product sum of `EuclideanSpace` and the synchronization of radii, and no theorem names `[FiniteDimensional ℝ E]`.  Nonemptiness in `IsNonemptyClosedInterval` is implicit in the book and follows from `x̄ ∈ C` anyway.  The binary `WithLp 2` formulas of [`ProductCones.lean`](RockafellarWets/Chapter6/ProductCones.lean) remain a consistency check, not an ingredient, since `WithLp 2 (E × F)` does not associate definitionally to `EuclideanSpace ℝ ι`. |
| 6.11 Gradient characterization of regular normals | **Exact** | [`GradientNormals.lean`](RockafellarWets/Chapter6/GradientNormals.lean) proves the pointwise characterization: `mem_regularNormalCone_of_isLocalMaxOn_hasGradientAt` is the sufficiency half, the gradient at a feasible relative local maximum being a regular normal, `exists_hasGradientAt_unique_isMaxOn_of_mem_regularNormalCone` is the necessity half with the book's `h₀`, and `mem_regularNormalCone_iff_exists_isLocalMaxOn_hasGradientAt` packages the equivalence.  [`SmoothGradientWitness.lean`](RockafellarWets/Chapter6/SmoothGradientWitness.lean) proves the strengthening: `exists_contDiff_one_hasGradientAt_unique_isMaxOn_of_mem_regularNormalCone` produces, for every regular normal `v`, a single `h : E → ℝ` with `ContDiff ℝ 1 h` on the whole space, `HasGradientAt h v x̄`, `IsMaxOn h C x̄` and `∀ y ∈ C, h y = h x̄ → y = x̄` -- the book's `smooth on IRⁿ`, its `smooth` meaning `C¹`, with the global maximum relative to `C` achieved uniquely at `x̄` -- and `mem_regularNormalCone_iff_exists_contDiff_one_unique_isMaxOn_hasGradientAt` restates the equivalence with this witness, its `⇐` half being the pointwise one after localizing the maximum with `IsMaxOn.localize`.  The construction is the printed one.  The modulus `θ₀(r) = sup{⟨v, y − x̄⟩ : y ∈ C, |y − x̄| ≤ r}` is a private `sSup` with `0` adjoined and `r` clamped to `max r 0`, which agrees with the book's for `x̄ ∈ C` and `r ≥ 0`; it is nondecreasing, zero at `0`, bounded by `r|v|` by Cauchy--Schwarz, and `o(r)` by the `ε`-`δ` form of 6(5).  `exists_contDiff_one_strict_majorant` of [`SmoothMajorant.lean`](RockafellarWets/Chapter6/SmoothMajorant.lean) -- the two averages `θ₁, θ₂` and the added `r²` of the printed proof -- turns it into `θ` with `θ(0) = 0`, `θ'(0) = 0` and `θ₀ < θ` on `(0, ∞)`, and the witness is `h(y) = ⟨v, y − x̄⟩ − θ(|y − x̄|)` exactly as printed: `h < 0 = h(x̄)` on `C ∖ {x̄}` from the strict majorization, and the gradient at `x̄` from `θ(t) = o(t)`, as for `h₀`.  **The book leaves the smoothness of `h` unproved**, listing only the requirements on `θ`; here it is the reusable `contDiff_one_comp_norm` and `contDiff_one_comp_norm_sub`, which say that a scalar `C¹` function with vanishing derivative at `0` composes with the norm of any real inner product space to a `C¹` function, with no evenness or second-order hypothesis.  Away from the centre this is the chain rule with `hasFDerivAt_norm_of_ne_zero`, the Hilbert-space derivative `‖y‖⁻¹⟨y, ·⟩` of the norm; at the centre, where the norm is not differentiable, the vanishing derivative gives the zero Fréchet derivative through the little-o estimate, and continuity of the derivative field there is the bound `‖(θ'(|y|)/|y|)⟨y, ·⟩‖ ≤ |θ'(|y|)| → |θ'(0)| = 0`.  The majorant used is `C¹` on all of `ℝ`, stronger than the book's `C¹` on `(0, ∞)` with `θ'(r) → 0` and `θ(r)/r → 0`.  Everything is dimension-free: the setting is `[InnerProductSpace ℝ E]`, with `[CompleteSpace E]` on the two gradient statements only because Mathlib's `HasGradientAt` is defined through the Riesz map -- automatic in `IRⁿ`, and the setting [`GradientNormals.lean`](RockafellarWets/Chapter6/GradientNormals.lean) already uses -- and no closedness, convexity or finite-dimensionality is asked anywhere.  `x̄ ∈ C` is the hypothesis of both equivalences, as Definition 6.3 presupposes and convention 6(8) requires, since `IsMaxOn h C x̄` does not force it and `N̂_C(x̄) = ∅` off `C`; the two forward constructions read it off `v ∈ N̂_C(x̄)`.  Figure 6-9 and the closing remark that a non-regular limiting normal is a limit of such gradients are not formalized. |
| 6.12 Basic first-order conditions for optimality | **Exact** | [`Optimality.lean`](RockafellarWets/Chapter6/Optimality.lean) proves every clause.  A differentiable local minimizer has nonnegative gradient pairing with every tangent direction; this is equivalent to membership of the negative gradient in the regular normal cone and implies membership in the limiting normal cone.  For convex `C`, `firstOrder_optimality_conditions_iff_of_convex` gives the tangent, regular-normal, limiting-normal, and global variational-inequality equivalences.  When the objective is also convex on `C`, `isMinOn_of_convexOn_of_firstOrder` proves the stated sufficiency for global optimality. |
| 6.13 Variational inequalities and complementarity | **Exact** | [`VariationalComplementarity.lean`](RockafellarWets/Chapter6/VariationalComplementarity.lean) proves every clause.  `IsVariationalSolution C F x̄` is `x̄ ∈ C ∧ -F(x̄) ∈ N_C(x̄)` with `N_C` the limiting normal cone of Definition 6.3; `isVariationalSolution_iff_zero_mem_singleton_add` is the printed relation `F(x̄) + N_C(x̄) ∋ 0`, the sum being the pointwise sum `{F(x̄)} + N_C(x̄)` of sets, and `isVariationalSolution_iff_neg_mem_normalCone` shows the feasibility conjunct is redundant by 6(8), normal cones being empty off `C`.  **The mapping is carried as a total map `F : E → E` rather than the printed `F : C → IRⁿ`**; only the single vector `F(x̄)` at a point of `C` ever enters, so nothing is lost, and no continuity, monotonicity, or gradient structure is assumed of `F`.  For convex `C`, `isVariationalSolution_iff_forall_inner_nonneg_of_convex` is the variational inequality `x̄ ∈ C, ⟨F(x̄), x − x̄⟩ ≥ 0 for all x ∈ C`, and `isVariationalSolution_iff_isMinOn_linear_of_convex` the reading that `l(x) = ⟨F(x̄), x⟩` attains its minimum over `C` at `x̄`, the conjunct `x̄ ∈ C` kept because Mathlib's `IsMinOn` does not assert membership; both come from 6.9 through `neg_mem_normalCone_iff_forall_inner_nonneg_of_convex` of [`Optimality.lean`](RockafellarWets/Chapter6/Optimality.lean), exactly as the printed Detail says, so this part is dimension-free and is stated over an arbitrary real inner product space with `Convex ℝ C` as its only hypothesis -- no closedness or nonemptiness of `C` is asked; `variationalCondition_equivalences_of_convex` packages the four forms.  The orthant `IRⁿ₊` is `euclideanNonnegativeOrthant = finiteBox (fun _ ↦ [0, ∞))` of Example 6.10 inside `EuclideanSpace ℝ ι` for an arbitrary `[Fintype ι]`, the empty index type included, and *not* the sup-normed `ι → ℝ`; its normal cone `normalCone_euclideanNonnegativeOrthant` is the specialization of `normalCone_finiteBox_of_closedIntervals` of [`FiniteBoxes.lean`](RockafellarWets/Chapter6/FiniteBoxes.lean), which, with the coordinate expansion of the Euclidean inner product, is where `[Fintype ι]` and hence finite-dimensionality enters.  The printed sentence *"this requires `v̄ⱼ = 0` when `x̄ⱼ > 0` but merely `v̄ⱼ ≥ 0` when `x̄ⱼ = 0`"* is `neg_mem_intervalNormalDirections_Ici_zero_iff`, the `Ici` case of the interval classification: at `0` the normal directions of `[0, ∞)` are `(−∞, 0]`, at a positive point `{0}`.  `isVariationalSolution_nonnegativeOrthant_iff` is then the coordinatewise complementarity condition `x̄ⱼ ≥ 0, F(x̄)ⱼ ≥ 0, x̄ⱼ F(x̄)ⱼ = 0` for every `j`, an actual equivalence for arbitrary `F`, and `isVariationalSolution_nonnegativeOrthant_iff_inner_eq_zero` is the vectorial summary `0 ≤ x̄ ⟂ F(x̄) ≥ 0`, both vectors in the orthant and `⟨x̄, F(x̄)⟩ = 0`, whose converse uses that a finite sum of nonnegative coordinate products vanishes only termwise.  The unnumbered remark that for `F = ∇f₀` the complementarity condition is the 6.12 optimality condition over `IRⁿ₊` is not separately packaged, being the instantiation of `isVariationalSolution_iff_neg_mem_normalCone` at a gradient. |
| 6.14 Normal cones to sets with constraint structure | Missing | |
| 6.15 Lagrange multipliers | Missing | |
| 6.16 Proximal normals | **Exact** | [`ProximalNormals.lean`](RockafellarWets/Chapter6/ProximalNormals.lean) defines `proximalNormalCone C x̄` by a positive projection step and proves the equivalent quadratic-support inequality.  `sub_mem_regularNormalCone_of_mem_projMapping` sends the displacement from a nearest point to a regular normal, and `nonneg_smul_sub_mem_regularNormalCone_of_mem_projMapping` covers every nonnegative multiple.  `projMapping_add_smul_eq_singleton_of_lt` proves the printed uniqueness assertion after moving any strictly smaller positive distance along the proximal-normal direction. |
| 6.17 Proximality of normals to convex sets | **Exact** | [`ProximalNormals.lean`](RockafellarWets/Chapter6/ProximalNormals.lean) proves `proximalNormalCone_eq_normalCone_of_convex` at every feasible point of a convex set.  `mem_normalCone_iff_mem_projMapping_add_of_convex` is the unit-step projection characterization, `projMapping_eq_svInv_add_normalCone_of_convex` is the set-valued identity `P_C = (I + N_C)⁻¹`, and `normalCone_eq_image_sub_svInv_projMapping_of_convex` records the equivalent identity `N_C = P_C⁻¹ - I`. |
| 6.18 Approximation of normals | **Exact** | [`NormalApproximation.lean`](RockafellarWets/Chapter6/NormalApproximation.lean) proves both parts, in each case with the approximating normals *proximal*.  (a) is `exists_proximalNormal_mem_normalCone_close_of_mem_normalCone`, literally the printed `x ∈ IB(x̄, ε) ∩ C` and `v ∈ IB(v̄, ε) ∩ N_C(x)` with `v` a proximal normal at `x`; `exists_proximalNormal_close_of_mem_normalCone` is the ball form without the `N_C(x)` clause, `exists_mem_proximalNormalCone_close_of_mem_normalCone` the norm form, and `exists_seq_mem_proximalNormalCone_tendsto_of_mem_normalCone` the density reading, proximal normals `vν → v̄` at points `xν → x̄` of `C`.  The proof is the Guide's projection step, `exists_mem_proximalNormalCone_projection_step`: any nearest point `x ∈ P_C(x̄ + δv̄)` carries the proximal normal `v = δ⁻¹(x̄ + δv̄ − x)` by construction, the squared nearest-point inequality gives `|x − x̄|² ≤ 2δ⟨v̄, x − x̄⟩`, and 6(5) at `x̄` turns this into `|x − x̄| ≤ δε/2` once `δ` is small enough that `|x − x̄| ≤ 2δ|v̄|` keeps `x` in the neighborhood where 6(5) applies; since `δ|v − v̄| = |x − x̄|`, that is the whole estimate.  **The Guide's normalization to `|v̄| = 1` is skipped**, the term `δ²|v̄|²` cancelling on the two sides of the nearest-point inequality, and the regular case `exists_mem_proximalNormalCone_close_of_mem_regularNormalCone` is lifted to `N_C(x̄)` by Definition 6.3 and one triangle inequality.  `[FiniteDimensional ℝ E]` enters (a) only through `projMapping_nonempty`, the existence of a nearest point of the closed set `C`.  (b) assumes of `Cν` **only the printed `lim sup_ν Cν = C`, written `outerSetLimit Cseq = C`, never Painlevé--Kuratowski convergence**: `exists_subsequence_mem_proximalNormalCone_tendsto_of_mem_normalCone` produces the strict extraction `φ`, points `xν ∈ C^{φ(ν)}` and proximal normals `vν` at `xν`, recorded also as members of `N_{C^{φ(ν)}}(xν)`, with `xν → x̄` and `vν → v̄`, and the displayed inclusion `N_C ⊂ g-lim sup_ν N_{Cν}` is `normalCone_subset_graphicalOuterLimit_normalCone`, pointwise in `x̄`, derived from its sharper proximal form `normalCone_subset_graphicalOuterLimit_proximalNormalCone`.  The proximal case follows the Guide: from `x̄ ∈ P_C(x̄ + τv̄)`, a sequence `yν ∈ C^{ψ(ν)}` with `yν → x̄` is read off `x̄ ∈ lim sup_ν Cν`, `exists_pkConvergent_subsequence` of [`SetConvergenceCompactness.lean`](RockafellarWets/Chapter4/SetConvergenceCompactness.lean) extracts from `C ∘ ψ` a subsequence converging to some `D`, which contains `x̄` and, as an outer limit along a subsequence, satisfies `D ⊆ lim sup_ν Cν = C`, so that `x̄ ∈ P_D(x̄ + τv̄)`; the inner half of Example 5.35, `svGraph_projMapping_subset_innerSetLimit` of [`ProjectionGraphicalConvergence.lean`](RockafellarWets/Chapter5/ProjectionGraphicalConvergence.lean), then supplies `(x̃ν, xν) ∈ gph P_{Cν}` converging to `(x̄ + τv̄, x̄)`, and `vν := τ⁻¹(x̃ν − xν)` is proximal at `xν` with the same `τ`.  **The Guide's diagonalization is replaced by closedness of the outer limit of the graphs**: for a general `v̄ ∈ N_C(x̄)` the density form of (a) gives proximal `(xν, vν) → (x̄, v̄)`, each pair lies in `gph g-lim sup_ν N_{Cν}` by the proximal case, and `isClosed_outerSetLimit` closes.  The parenthetical clause, that the index set is `ℕ` itself when `Cν → C` actually holds, is `exists_seq_mem_proximalNormalCone_tendsto_of_pkConverges`, the same argument run with `D = C` and no extraction, the graphical *inner* limit replacing the outer one (`normalCone_subset_graphicalInnerLimit_proximalNormalCone`), the finitely many indices before the inner limit takes effect being filled with a point `zν ∈ Cν` and the zero proximal normal there -- **the only place the printed `Cν ≠ ∅` is used**.  In the main theorem it is carried as an unused hypothesis, `x̄ ∈ lim sup_ν Cν` already producing points of infinitely many `Cν`, as are `x̄ ∈ C`, which `v̄ ∈ N_C(x̄)` implies, and closedness of `C`, which `C = lim sup_ν Cν` implies.  `[FiniteDimensional ℝ E]` enters (b) through the second countability behind the extraction, through the inner half of 5.35, and through (a); strict convexity of the norm, which 5.35 needs, is automatic in an inner product space.  Nothing asserts graphical convergence of the `N_{Cν}`: the reverse inclusion is not claimed, and the book's example of strictness, `Cν = gph(ν⁻¹ sin(νx₁))`, is not formalized. |
| 6.19 Characterization of boundary points | Missing | |
| 6.20 Envelope representation of convex sets | **Exact** | [`SupportingHalfspaces.lean`](RockafellarWets/Chapter6/SupportingHalfspaces.lean) defines supporting halfspaces and, in finite dimensions, separates every exterior point of a nonempty closed convex set and proves `eq_sInter_supportingHalfspaces`.  The converse closedness and convexity of arbitrary intersections, their indexed and empty-family forms, and the homogeneous through-the-origin envelope for closed convex cones are all present.  `isClarkeRegularAt_of_closed_convex` proves the concluding regularity clause. |
| 6.21 Polarity correspondence | **Exact** | [`Polarity.lean`](RockafellarWets/Chapter6/Polarity.lean) defines the book's nonpositive `polarCone`, proves that it is a closed convex cone and reverses inclusions, and shows that closure and convex-conic hull do not change it.  `polarCone_bipolar` identifies the bipolar with `closure (conicHull K)`; consequently closed convex cones are fixed by bipolarization and polarity is involutive and injective on them.  The singleton, whole-space, ray, and homogeneous-halfspace formulas are also supplied. |
| 6.22 Pointedness and polarity | **Exact** | [`Polarity.lean`](RockafellarWets/Chapter6/Polarity.lean) proves in finite dimensions that a convex cone has nonempty interior exactly when its polar is pointed, and that `w` is interior exactly when every nonzero polar vector pairs strictly negatively with it.  The printed replacement clause is also proved for a closed cone `K₀`: strict testing may use the nonzero vectors of `K₀` itself, and `interior (polarCone K₀)` is nonempty exactly when `K₀` is pointed.  No convexity hypothesis is added to that replacement clause. |
| 6.23 Orthogonal subspaces | **Exact** | [`Polarity.lean`](RockafellarWets/Chapter6/Polarity.lean) proves `polarCone (M : Set E) = (Mᗮ : Set E)` for every real inner-product subspace `M`, and records the finite-dimensional double-complement identity `Mᗮᗮ = M`. |
| 6.24 Polarity of normals and tangents to convex sets | **Exact** | [`Polarity.lean`](RockafellarWets/Chapter6/Polarity.lean) proves, for convex `C` and `x̄ ∈ C`, that `normalCone C x̄` is the polar of `tangentCone C x̄`; finite-dimensional bipolarity gives the converse identity.  `isPointed_normalCone_iff_interior_nonempty` proves that the normal cone is pointed exactly when `interior C` is nonempty. |
| 6.25 Regular tangent vectors | **Exact** | [`RegularTangents.lean`](RockafellarWets/Chapter6/RegularTangents.lean) defines `regularTangentCone C x̄` by formula 6(15), the joint inner limit of `blowUp C x τ` as the base approaches through `C` and the positive scale tends to zero.  `mem_regularTangentCone_iff_forall_sequences` proves the exact printed sequential form: for every positive `τν → 0` and every `x̄ν →_C x̄`, points `xν ∈ C` can be chosen with `xν → x̄` and the required difference quotients tending to `w`. |
| 6.26 Regular tangent cone properties | **Exact** | [`RegularTangents.lean`](RockafellarWets/Chapter6/RegularTangents.lean) proves that the regular tangent cone is closed and, at a point of `C`, is a convex cone contained in the derivable and ordinary tangent cones.  The sequence/ball failure forms 6(17)--6(18) are formalized.  Under the book's finite-dimensional and pointwise local-closedness hypotheses, `regularTangentCone_eq_svInnerLimitWithin_tangentCone` proves formula 6(16), and `mem_regularTangentCone_iff_forall_tangent_sequences` gives its exact sequential reading. |
| 6.27 Normals to tangent cones | Missing | |
| 6.28 Tangent-normal polarity | Missing | |
| 6.29 Characterizations of Clarke regularity | **Partial** | [`FiniteProductCones.lean`](RockafellarWets/Chapter6/FiniteProductCones.lean) proves only the implication (a) ⇒ (b): `tangentCone_eq_regularTangentCone_of_isClarkeRegularAt` gives `T_C(x̄) = T̂_C(x̄)` at any `x̄ ∈ C` where `C` is regular in the sense of Definition 6.4, under `[FiniteDimensional ℝ E]`, by the second half of the book's argument for 6.28(b) with a private projection lemma in place of 6.27(b).  It is there as infrastructure for the last clause of 6.41.  **The converse (b) ⇒ (a), the polar characterizations (c)--(e), and the semicontinuity characterizations (f)--(g) are not formalized**, and nothing of 6.28 or 6.30 is stated. |
| 6.30 Consequences of Clarke regularity | Missing | |
| 6.31 Tangent cones to sets with constraint structure | Missing | |
| 6.32 Regular tangents under a change of coordinates | Missing | |
| 6.33 Recession vectors | **Exact** | [`RecessionCones.lean`](RockafellarWets/Chapter6/RecessionCones.lean) defines `localRecessionCone C x̄` exactly as printed -- `x̄ ∈ C` together with some `V ∈ 𝓝 x̄` and `ε > 0` such that `x + τ • w ∈ C` for all `x ∈ C ∩ V` and `τ ∈ [0, ε]` -- and `globalRecessionCone C` by invariance of every forward ray, `x + τ • w ∈ C` for all `x ∈ C` and `τ ≥ 0`.  `mem_localRecessionCone_iff_exists_ball` and `mem_localRecessionCone_iff_eventually` are the metric and filter readings of the neighborhood, and `mem_globalRecessionCone_iff_add_smul_singleton_subset` is the sentence after the definition, `C + τw ⊆ C` for all `τ ≥ 0`.  **The printed `at a point x̄ ∈ C` is carried definitionally, as convention 6(8) is for the normal cones**: the conjunct `x̄ ∈ C` makes `localRecessionCone C x̄ = ∅` off `C` (`localRecessionCone_eq_empty_of_not_mem`), so membership in `R_C(x̄)` already carries feasibility and every `x̄ ∈ C` asked for in 6.34 is the book's own.  The global set is the ray condition alone, so `globalRecessionCone ∅ = univ`, which is what makes it agree with the empty intersection in 6.34(b).  The module is stated over `[NormedAddCommGroup E] [NormedSpace ℝ E]` with no finite-dimensional hypothesis anywhere; nothing in 6.33--6.34 needs one.  The informal `recedes within itself' remark and Figure 6-18 are not formalized. |
| 6.34 Recession cones and convexity | **Adapted** | [`RecessionCones.lean`](RockafellarWets/Chapter6/RecessionCones.lean) proves every clause, dimension-free, with two deviations in (c) recorded below.  (a): at `x̄ ∈ C`, `isCone_localRecessionCone` and `convex_localRecessionCone` make `R_C(x̄)` a convex cone, and `localRecessionCone_subset_regularTangentCone` puts it inside the *regular* tangent cone `T̂_C(x̄)`: given `x̄ν →_C x̄` and `τν ↓ 0`, the points `x̄ν + τν w` lie in `C` once `x̄ν` is in the witness neighborhood and `τν ≤ ε`, and realize the difference quotient `w` exactly, so the sequential form of 6.25 applies; `localRecessionCone_subset_derivableCone` and `localRecessionCone_subset_tangentCone` follow from 6.26.  The uniform finite-family clause is `exists_nhds_forall_singleton_add_smul_convexHull_subset`, in the printed form `{x} + τ • con{wᵢ} ⊆ C` for a family indexed by an arbitrary finite type, with `exists_nhds_forall_smul_mem_of_mem_convexHull` for a finite set `W ⊆ R_C(x̄)`; `C` is arbitrary, and convexity of `R_C(x̄)` is deduced as the two-direction case rather than assumed.  The engine `exists_pos_forall_add_sum_smul_mem` applies the finitely many directions successively under one threshold on the total step, carrying a distance bound so that each intermediate point stays inside the witness neighborhood of the direction applied next, and `mem_convexHull_range_iff` writes a point of the hull as a probability combination.  (b): `globalRecessionCone_eq_iInter_localRecessionCone` is the printed equality for closed `C`, the easy inclusion holding for every set; the reverse is the printed last-contact argument -- the set of parameters `σ ∈ [0, τ]` with `x + σ • w ∈ C` is compact, its supremum is a contact point, and local recessiveness there produces contact beyond the supremum.  Closedness of `C` is used exactly there and in `isClosed_globalRecessionCone`, an intersection of closed preimages; `isCone_globalRecessionCone` and `convex_globalRecessionCone` need nothing, `convex_iInter_localRecessionCone`, `isCone_iInter_localRecessionCone` and `isClosed_iInter_localRecessionCone` record the same for the intersection, and `iInter_localRecessionCone_empty` with `globalRecessionCone_empty` fix the empty-set convention on both sides as `univ`.  (c): `horizonCone_subset_globalRecessionCone` is the closed convex reverse inclusion through 3.6 (`smul_add_mem_of_mem_horizonCone`), and `globalRecessionCone_eq_horizonCone` and `iInter_localRecessionCone_eq_horizonCone` are the printed equation.  **The inclusion in `C^∞` carries `C.Nonempty`**, in `globalRecessionCone_subset_horizonCone` (which does not need convexity) and in the printed-hypothesis form `globalRecessionCone_subset_horizonCone_of_convex`: under the rule of Definition 3.3 that `C^∞ = {0}` for `C = ∅` -- the book's rule, which it says is `dictated' rather than a convention -- the vacuous global recession set of `∅` is `univ`, so the printed inclusion fails for the empty convex set; the simp lemmas `globalRecessionCone_empty` and `horizonCone_empty` are the two halves of that counterexample, and the hypothesis is what the Guide's appeal to 3.6, stated at a point `x̄ ∈ C`, silently uses.  **(c) is proved for the ray-defined `globalRecessionCone`, not for the literal `⋂ₓ R_C(x)` the exercise writes**: the two agree only under the closedness of (b), and for an open convex `C` every vector is locally recessive at every point, so the literal intersection is `univ` while `C^∞` can be `{0}`; that counterexample is explained in the module docstring but not formalized. |
| 6.35 Recession vectors from tangents and normals | Missing | |
| 6.36 Interior tangents and recession | Missing | |
| 6.37 Internal approximation in tangent limits | Missing | |
| 6.38 Convexified normal and tangent cones | Missing | |
| 6.39 Constraint qualification in tangent cone form | Missing | |
| 6.40 Mangasarian-Fromovitz constraint qualification | Missing | |
| 6.41 Tangents and normals to product sets | **Exact** | [`FiniteProductCones.lean`](RockafellarWets/Chapter6/FiniteProductCones.lean) proves every clause for an arbitrary finite index type.  The product is the Euclidean `L²` product `PiLp 2 E` of a family `E : ι → Type*` with `[Fintype ι]`, and `l2PiSet C = {x | ∀ i, x i ∈ C i}` is the product set, so heterogeneous factors and the empty index type are covered and each clause is dimension-free unless said otherwise.  `normalCone_l2PiSet` and `regularNormalCone_l2PiSet` are the two normal-cone *equalities*, stated at every base point (off the product both sides are empty by 6(8)) and **without closedness of the factors**: forward, the single-coordinate variation `l2PiUpdate x i y` satisfies `‖l2PiUpdate x i y − x‖ = ‖y − x i‖` and pairs only through coordinate `i`, which is where the Euclidean rather than the sup norm is used; backward, `⟨v, z − x⟩ = ∑ᵢ ⟨vᵢ, zᵢ − xᵢ⟩` with `ε/(card ι + 1)` distributed over the coordinates and `‖zᵢ − xᵢ‖ ≤ ‖z − x‖`, and for limiting normals the finitely many witness sequences are zipped into one product sequence.  `tangentCone_l2PiSet_subset` is the printed inclusion, by continuous coordinate evaluation, and `derivableCone_l2PiSet` adds the derivable-cone equality of the binary module.  `regularTangentCone_l2PiSet` is the canonical regular-tangent equality, proved from Definition 6.25 through the exact sequential form `mem_regularTangentCone_iff_forall_sequences` of [`RegularTangents.lean`](RockafellarWets/Chapter6/RegularTangents.lean) instead of the polarity 6.28 of the printed proof: forward, a base sequence in one factor is frozen into the product by `l2PiUpdate`; backward, the finitely many selections are made against the *same* base sequence and scales and zipped.  **It therefore needs neither closedness nor finite dimension**, only `x̄ ∈ C`.  `isClarkeRegularAt_l2PiSet_iff` is the regularity equivalence under the printed closedness of every factor, and `tangentCone_l2PiSet_of_isClarkeRegularAt` is the final clause from Clarke regularity of the factors alone, as printed, not from geometric derivability and with no separate closedness hypothesis since Definition 6.4 carries local closedness.  It routes through `T̂`: the product formula for `T̂` and the bridge `tangentCone_eq_regularTangentCone_of_isClarkeRegularAt`, which is the implication 6.29(a) ⇒ (b) and the one place `[∀ i, FiniteDimensional ℝ (E i)]` is asked for, through formula 6(16) and compactness of closed balls in each factor.  The bridge is proved by the second half of the book's argument for 6.28(b), with a private projection lemma standing in for 6.27(b): blowing up nearest points of `C` to `x̄ + τw` produces `v ∈ N_C(x̄)` with `‖v‖ = d(w, T_C(x̄))` and `⟨v, w⟩ = d(w, T_C(x̄))²`, the conic minimality `⟨w − q, q⟩ = 0` of the limiting quotient turning the distance into a pairing; no other part of 6.27--6.30 is claimed.  `closure_conicHull_tangentCone_l2PiSet` is the unnumbered display after the example, `cl con T_C(x̄) = ∏ᵢ cl con T_{Cᵢ}(x̄ᵢ)`, obtained as the book says by taking the polar of the regular-normal formula through `polarCone_l2PiSet` and `polarCone_bipolar` of [`Polarity.lean`](RockafellarWets/Chapter6/Polarity.lean), using finite dimension for the `⇐` half of 6(6) and for completeness.  The locality of `N̂_C(x̄)` under intersection with a neighborhood is taken from [`ElementaryCones.lean`](RockafellarWets/Chapter6/ElementaryCones.lean), and the binary `WithLp 2` module [`ProductCones.lean`](RockafellarWets/Chapter6/ProductCones.lean) is used unchanged.  The strictness example `C₁ = C₂ = {0} ∪ {2⁻ᵏ}` is not formalized. |
| 6.42 Tangents and normals to intersections | Missing | |
| 6.43 Tangents and normals to image sets | Missing | |
| 6.44 Tangents and normals under set addition | Missing | |
| 6.45 Polars of polyhedral cones; Farkas | **Exact** | [`FarkasPolarCones.lean`](RockafellarWets/Chapter6/FarkasPolarCones.lean) proves the printed identity as the set equality `polarCone_setOf_inner_nonpos_eq_conicHull_range`: for any `a : Fin m → E`, the polar of `K = {x : ⟨aᵢ, x⟩ ≤ 0 for all i}` is `conicHull (Set.range a)`, with the nonpositive `polarCone` of 6(14) and the Chapter 3 `conicHull`, which is the book's `con pos` by `conicHull_eq_convexHull_positiveHull`; `polarCone_setOf_inner_nonpos_eq_conicHull_image` restates it with the generators as a `Finset`.  The clause identifying `con pos{a₁, …, aₘ}` with the combinations `y₁a₁ + ⋯ + yₘaₘ`, `yᵢ ≥ 0`, is `conicHull_range_eq` and `mem_conicHull_range_iff`, with multipliers `y : Fin m → ℝ` rather than finitely supported coefficients, so that repeated generators are absorbed by the indexing; together they give the Farkas alternative `mem_polarCone_setOf_inner_nonpos_iff` and, stated without polarity, `forall_inner_nonpos_iff_exists_nonneg_multipliers`: `⟨v, x⟩ ≤ 0` for every solution of `⟨aᵢ, x⟩ ≤ 0` exactly when `v = ∑ yᵢaᵢ` with `yᵢ ≥ 0`.  The proof is the printed one.  `J* = K` for `J = conicHull (range a)` is `polarCone_conicHull_range`, from `polarCone_conicHull` (6.21) and `polarCone_range`, and is dimension-free; `isClosed_conicHull_range` is the closedness half of 3.52, `isClosed_conicHull_finset` of [`GeneratedCones.lean`](RockafellarWets/Chapter3/GeneratedCones.lean), transported along `range a = ↑(univ.image a)`; and `K* = J** = J` is `polarCone_bipolar_eq_self` of [`Polarity.lean`](RockafellarWets/Chapter6/Polarity.lean).  `[FiniteDimensional ℝ E]` is used exactly where the book uses it, for closedness of the finitely generated cone, and it also supplies the completeness the bipolar theorem asks for, through the properness of finite-dimensional real spaces.  **No hypothesis is placed on the family**: `m = 0`, zero vectors, and repeated vectors are all allowed, the book's `aᵢ ≠ 0` being only its proof convenience, and for `m = 0` both sides reduce to `{0}`.  The title's polyhedral cones are tied to Chapter 3 by `homogeneousLinearInequalitySet_innerSL`, which identifies `K` with `homogeneousLinearInequalitySet (fun i => innerSL ℝ (a i))`, so `isHPolyhedralCone_setOf_inner_nonpos` makes `K` H-polyhedral, `isFinitelyGeneratedCone_polarCone_setOf_inner_nonpos` and `isClosed_polarCone_setOf_inner_nonpos` make `K*` finitely generated and closed, and conversely `IsFinitelyGeneratedCone.exists_polarCone_setOf_inner_nonpos` and `IsFinitelyGeneratedCone.isHPolyhedralCone_polarCone` describe the polar of any finitely generated cone by the homogeneous inequalities in its generators, both without finite-dimensionality.  In finite dimensions polarity then preserves and exchanges both classes -- `IsFinitelyGeneratedCone.isFinitelyGeneratedCone_polarCone`, `IsHPolyhedralCone.isFinitelyGeneratedCone_polarCone`, `IsHPolyhedralCone.isHPolyhedralCone_polarCone` -- through the integrated Minkowski--Weyl theorem of [`MinkowskiWeyl.lean`](RockafellarWets/Chapter3/MinkowskiWeyl.lean) rather than a second induction; and since Chapter 3's H-description uses continuous functionals, `IsHPolyhedralCone.exists_inner_representation` recovers the printed form `{x : ⟨aᵢ, x⟩ ≤ 0}` by Riesz representation in any complete space, giving `IsHPolyhedralCone.exists_polarCone_eq_conicHull_range` as the lemma for an arbitrary H-polyhedral cone.  No separation argument is reproved. |
| 6.46 Tangents and normals to polyhedral sets | Missing | |
| 6.47 Exactness of tangent approximations | Missing | |
| 6.48 Separation of convex cones | Missing | |
| 6.49 Generic continuity of normal-cone mappings | **Adapted** | [`GenericNormalContinuity.lean`](RockafellarWets/Chapter6/GenericNormalContinuity.lean) explains why the printed arbitrary-set frontier statement is false under convention 6(8).  For `C = ℚ ⊂ ℝ`, `frontier C = ℝ`; normal cones are empty at irrational points but contain zero at arbitrarily close rational points, so outer semicontinuity fails off `C`, while inner semicontinuity fails on `C`, making every point exceptional.  The hypothesis-free corrected theorem works relative to `C ∩ frontier C` and proves meagerness of the discontinuity set there in finite dimensions.  When `C` is closed, `frontier C ⊆ C`, so the module recovers the full printed conclusion on `frontier C`: outer semicontinuity, meagerness of discontinuities, and density of continuity points. |
