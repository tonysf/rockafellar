# Chapter 5 Coverage Ledger

This ledger tracks Chapter 5 of Rockafellar--Wets, *Set-Valued Mappings*,
result by result. `Exact` means that every substantive mathematical clause is
available under the project's stated finite-dimensional real-space
assumptions. `Adapted` records a mathematically necessary change to a printed
statement, together with a formal explanation or counterexample. `Partial`
records a sound API for only part of the printed result, with the remaining
clauses named. `Missing` means that no result-specific API has yet been
integrated.

Formalization covers all 59 numbered results, 5.1 through 5.59, Sections A--J
in full.  The three introductory examples are in
[`IntroductoryExamples.lean`](RockafellarWets/Chapter5/IntroductoryExamples.lean),
which comes last in the import order rather than first: the book's
`S(x) = {F(x)}` for `x ∈ X` is `svRestrict (svSingleton F) X` and its implicit
and algorithmic mappings run on the composition `svComp` of 5.52, so the
examples are stated with machinery the later sections supply rather than with
definitions of their own.  Michael's selection theorem, which 5.58 and 5.59 rest on, is not in
Mathlib under that name, but the partition-of-unity construction at its
centre is: `exists_continuous_forall_mem_convex_of_local_const` is the book's
Parts 1 and 4 at once, and in more generality than the book states them.
What was left to build is the book's Parts 2, 3 and 5, in
[`MichaelSelection.lean`](RockafellarWets/Chapter5/MichaelSelection.lean) and
[`MichaelRepresentation.lean`](RockafellarWets/Chapter5/MichaelRepresentation.lean).

Sections A--D are filter-native: formula 5(1) is a limit along the
neighborhood filter, and the sequential criteria rest on
[`SequentialLimits.lean`](RockafellarWets/Chapter5/SequentialLimits.lean),
which transports the Chapter 4 limits along `atTop` to those limits and back.
It is written as reusable machinery rather than inline to 5.6, since later
results in the chapter are also stated with sequences.

Sections E--G are sequential, the pointwise and graphical limits being the
Chapter 4 `atTop` limits applied to values and to graphs.  Section G runs
throughout on the `ε`-`ρ` *inclusions* of 4.10 rather than on distance
functions: continuous convergence, uniform convergence, continuity of the
limit and equicontinuity all have an inclusion form here, and the whole
section is the composition of two inclusions through a third set, isolated as
`inter_closedBall_subset_thickening_trans`.  That is shorter than the book's
route, which translates everything through 5.42 into scalar functions, and it
is why 5.43 and 5.46 do not depend on 5.42.

Section J states the book's relative notions -- nowhere dense in `X`, meager
in `X`, `intₓ`, `clₓ` -- inside the subspace `↥X`, where they are Mathlib's
ordinary `IsNowhereDense`, `IsMeagre`, `interior` and `closure`.  That choice
follows the two 5.7 preimage criteria, which were already stated as
`IsClosed (X ↓∩ S⁻¹(B))` and now as `IsOpen (X ↓∩ S⁻¹(O))`; the bridge in
either direction is `preimage_val_mem_nhds_iff` and `mem_closure_preimage_val`
in [`SemicontinuityCriteria.lean`](RockafellarWets/Chapter5/SemicontinuityCriteria.lean).
No relative versions of the predicates are defined, and the two conventions
are not mixed.  The same convention carries 5.58 and 5.59: Michael's theorem
is built on `↥X`, where inner semicontinuity is absolute and there is no
`WithinAt` bookkeeping, and `svIscAt_subtype_iff` transports it to and from
the book's relative statements.

| Status | Count |
| --- | ---: |
| Exact | 50 |
| Adapted | 8 |
| Partial | 1 |
| **Total** | **59** |

| Result | Status | Lean coverage / remaining work |
|---|---|---|
| 5.1 Constraint systems | **Exact** | [`IntroductoryExamples.lean`](RockafellarWets/Chapter5/IntroductoryExamples.lean) proves every identification the example makes about the mapping `S(x) = {F(x)}` for `x ∈ X`, `∅` otherwise, which the paragraph before it introduces and which is `svRestrict (svSingleton F) X` in the machinery of 5.20 and 5.21.  Its domain is `X`; its values are subsingletons everywhere, which is the book's "isn't multivalued anywhere"; `F⁻¹(u)` is the solution set `{x ∈ X | F(x) = u}` of the equation system; the set-valued preimage is `X ∩ F⁻¹(D)` for *any* `D`, of which the box case `{x ∈ X | fᵢ(x) ∈ Dᵢ}` is a corollary; and the range of the inverse is `X`.  Coordinates are carried by an arbitrary index type and a dependent family of targets, so nothing depends on the number of equations or unknowns being finite or equal.  The example's closing question -- whether `F⁻¹` might be single-valued when `m = n` -- is settled rather than left open: `subsingleton_svInv_svRestrict_svSingleton_iff` shows the inverse is subsingleton-valued exactly when `F` is injective on `X`, a condition equality of the dimensions neither implies nor requires. |
| 5.2 Generalized equations and implicit mappings | **Exact** | [`IntroductoryExamples.lean`](RockafellarWets/Chapter5/IntroductoryExamples.lean) defines the implicit mapping `svImplicit S ū w = {x | S(x, w) ∋ ū}` and proves the three readings the example gives it: at a fixed parameter it is the inverse image `S(·, w)⁻¹(ū)`, which with no parameter is the example's first sentence that the solution set of `S(x̄) ∋ ū` is `S⁻¹(ū)`; its graph is the `ū`-level set of `S` with the coordinates exchanged; and for `S(x, w) = F(x, w) + N(x)` the generalized equation `S(x, w) ∋ 0` is the printed special case `-F(x, w) ∈ N(x)`.  The operations paragraph that follows the example is covered in the parts not already carried by [`PerturbedMappings.lean`](RockafellarWets/Chapter5/PerturbedMappings.lean) and [`MappingOperations.lean`](RockafellarWets/Chapter5/MappingOperations.lean), where `svAdd` and `svComp` are defined for 5.51 and 5.52: both displayed descriptions of the composition, as `⋃_{u ∈ S(x)} T(u)` and as `{w | S(x) ∩ T⁻¹(w) ≠ ∅}`, the identity `(T ∘ S)⁻¹ = S⁻¹ ∘ T⁻¹`, and the reduction to ordinary composition on single-valued mappings. |
| 5.3 Algorithmic mappings and fixed points | **Exact** | [`IntroductoryExamples.lean`](RockafellarWets/Chapter5/IntroductoryExamples.lean) defines `svFixedPoints S = {x | x ∈ S(x)}` and the iterates `svIterate S ν`, with `S⁰(x) = {x}` so that `S¹ = S`, and proves the inclusion the example displays: a sequence generated from `x⁰` by `xν ∈ S(xν₋₁)` has `xν ∈ (S ∘ ⋯ ∘ S)(x⁰)`.  A fixed point is recovered as the constant such sequence, so it lies in every iterate of itself.  **Two convergence statements are added**, since the example's own closing paragraph says that this framework is what makes both the continuity notions and the convergence of a *sequence* of mappings matter, and each is one line over machinery already present: a convergent generated sequence has its limit in that limit's outer limit 5(1), so the limit is a fixed point as soon as `S` is osc there; and for algorithmic mappings `Tν` the pairs `(xν, xν₊₁)` along the shifted index put the limit in the graphical outer limit of the `Tν` at itself, so it is a fixed point of any `S` containing that limit -- which is exactly the hypothesis 5.37(a) is stated with. |
| 5.4 Continuity and semicontinuity | **Exact** | [`SetLimitsAlong.lean`](RockafellarWets/Chapter5/SetLimitsAlong.lean) restates the Definition 4.1 limits along an arbitrary index filter; [`Semicontinuity.lean`](RockafellarWets/Chapter5/Semicontinuity.lean) defines formula 5(1) along the full neighborhood filter, defines osc, isc, and continuity both absolutely and relative to a set, and proves the equalities the book records immediately afterwards: `lim sup = S(x̄)` under osc, closedness of `S(x̄)` under osc both absolutely and relative to a set `X`, the closed-valued equality form of isc, and that isc at a point of the domain puts a whole neighborhood inside the domain. |
| 5.5 Profile mappings | **Exact** | [`ProfileMappings.lean`](RockafellarWets/Chapter5/ProfileMappings.lean) defines `epiProfile` and `hypoProfile` over the Chapter 1 extended-real conventions and proves `gph Ef = epi f`, `dom Ef = dom f`, `Ef⁻¹(α) = lev≤α f`, that `Ef` is osc at `x̄` iff `f` is lsc at `x̄`, isc iff `f` is usc, continuous iff `f` is continuous, and that the level-set mapping `α ↦ lev≤α f` is osc everywhere iff `f` is lsc everywhere. The hypographical analogues `gph Hf = hypo f`, `dom Hf`, `Hf⁻¹(α) = lev≥α f`, osc iff usc, and isc iff lsc are proved as well.  Each of the four semicontinuity equivalences is proved once along an arbitrary index filter, in a form that uses no topology on the domain, so that the absolute statements and the statements relative to a set `X` -- which 5.56 needs -- are both instances of the same core.  Closed-valuedness of the profiles is recorded as `isClosed_epiProfile` and `isClosed_hypoProfile`. |
| 5.6 Criteria for semicontinuity at a point | **Exact** | [`SemicontinuityCriteria.lean`](RockafellarWets/Chapter5/SemicontinuityCriteria.lean) proves the neighborhood criteria (a) and (b) in the book's `X ∩ V ∩ S⁻¹(W)` form, and the sequential criteria (c) and (d) quantified over all `xν ∈ X` with `xν → x̄` and `S(xν) → D`, relative to `X` and in the absolute case. The sequential clauses go through the diagonal extraction of [`SequentialLimits.lean`](RockafellarWets/Chapter5/SequentialLimits.lean) followed by the subsequence compactness of 4.18. |
| 5.7 Characterizations of semicontinuity | **Exact** | [`SemicontinuityCriteria.lean`](RockafellarWets/Chapter5/SemicontinuityCriteria.lean) proves (a) in full -- osc everywhere is closedness of `gph S`, and `S` is osc exactly when `S⁻¹` is; (b) for closed-valued `S`, as `IsClosed (X ↓∩ S⁻¹(B))` for every compact `B` and equivalently as the closure condition `X ∩ cl(X ∩ S⁻¹(B)) ⊂ S⁻¹(B)`, with the absolute case `X = IRⁿ` separately; and (c) in both the absolute form (`S⁻¹(O)` open for open `O`) and the relative form. The forward half of (b) needs no structure on the target; the converse uses compactness of closed balls and is stated for a proper metric target.  Clause (c) is also stated in the subtype form `IsOpen (X ↓∩ S⁻¹(O))` matching (b), which is what Section J consumes. |
| 5.8 Feasible-set mappings | **Exact** | [`FeasibleSets.lean`](RockafellarWets/Chapter5/FeasibleSets.lean) defines the parameterized constraint mapping `T`, empty off the parameter set `W`, and proves that `gph T` is closed and hence `T` is osc when `X` and `W` are closed and the constraint functions are continuous on `X × W`; that `T` is osc at every interior point of `W` when `W` is not closed, via the locality of osc and a closed neighborhood inside `W`; and that `dom T` is the set of `w ∈ W` whose constraint system is consistent. |
| 5.9 Inner semicontinuity from convexity | **Exact** | [`ConvexSemicontinuity.lean`](RockafellarWets/Chapter5/ConvexSemicontinuity.lean) defines convex-valuedness and graph-convexity, proves formula 5(4), and proves all three clauses: (a) in the general relative form with the specializations to `dom S` and to the absolute "in particular" statement `(x̄, u) ∈ int(gph S)`, via 4.15 in one direction and closedness of inner limits with `cl(int C) = cl C` in the other; (b) via 4.32(c) applied to the fibre `{x̄} × IRᵐ` intersected with `gph S`, the non-separation hypothesis being interiority of `x̄` in `dom S`; (c) via the inner-limit half of 4.30, both absolutely and relative to a set. |
| 5.10 Parameterized convex constraints | **Exact** | [`ConvexConstraints.lean`](RockafellarWets/Chapter5/ConvexConstraints.lean) proves the printed conclusion: a Slater point at `w₀` yields an explicit open neighborhood of `w₀` on which `T` is continuous. The supporting assertions of the Detail are proved too -- closed graph and hence osc everywhere (needing continuity only, no convexity), convexity of each `T(w)`, openness of the strictly feasible set, and the interior identity `int T(w) = {x | fᵢ(x,w) < 0}` that the book takes from 2.34. Since 2.34 is not available in this project's Chapter 2, inner semicontinuity is obtained instead from density of the strictly feasible set in `T(w)`, proved directly from the Slater segment; the interior identity is then derived from the same computation rather than assumed. |
| 5.11 Continuity of distances | **Exact** | [`DistanceCriteria.lean`](RockafellarWets/Chapter5/DistanceCriteria.lean) proves (a), (b), and (c) relative to a set `X` and in the absolute case, with distances taken as `Metric.infEDist` so that `d(u, ∅) = ∞` as the book requires. Clause (b) needs neither closed-valuedness nor properness; only (a) uses closedness of `S(x̄)` and compactness of closed balls, the latter through the new filter-native compact extraction `outerSetLimitAlong_inter_nonempty_of_frequently` in [`SetLimitsAlong.lean`](RockafellarWets/Chapter5/SetLimitsAlong.lean), which replaces the subsequence step that 4.7 supplies only for sequences. The printed clause (b) reads `u → d(u, S(x))`; this is a misprint for `x → d(u, S(x))`, as clauses (a) and (c) make clear, and the corrected reading is what is proved.  The real-valued form of (c), available on nonempty values where the extended distance is finite, is `continuousWithinAt_infDist_of_svContinuousWithinAt` in [`ContinuousSelections.lean`](RockafellarWets/Chapter5/ContinuousSelections.lean), which 5.57 runs on. |
| 5.12 Uniformity of approximation in semicontinuity | **Exact** | [`UniformSemicontinuity.lean`](RockafellarWets/Chapter5/UniformSemicontinuity.lean) proves (a) and (b) both in the eventual form along `𝓝[X] x̄` and in the book's explicit `∃ V ∈ N(x̄), ∀ x ∈ X ∩ V` form. The book's derivation from 4.10 does not transfer, since this project's 4.10 proofs extract convergent subsequences and no subsequence is available along a neighborhood filter; (a) instead uses the compact extraction `outerSetLimitAlong_inter_nonempty_of_frequently` applied to `ρIB \ thickening ε (S x̄)`, and (b) a finite subcover of the compact set `S(x̄) ∩ ρIB`. Following Chapter 4, `S + εIB` is the open thickening; the `∀ ε > 0` quantifier makes this equivalent to the closed one. |
| 5.13 Uniform continuity | **Exact** | [`UniformSemicontinuity.lean`](RockafellarWets/Chapter5/UniformSemicontinuity.lean) proves the uniform inclusion over a compact `X`, following the book's guide: both clauses of 5.12 at each point of `X` give an open cover, and a Lebesgue number for it is the required `δ`. Two hypotheses of the printed statement are shown to be unnecessary rather than assumed -- `X ⊂ dom S` is not used, since outer semicontinuity already drives the nearby images out of every bounded ball where `S(x̄)` is empty, and closed-valuedness is automatic from outer semicontinuity relative to `X`. The inner clause of 5.12 must be invoked at the enlarged radius `ρ + ε`, since the intermediate point it produces need not lie in `ρIB`. |
| 5.14 Local boundedness | **Exact** | [`LocalBoundedness.lean`](RockafellarWets/Chapter5/LocalBoundedness.lean) introduces the set image `svImage S V = ⋃ {S(x) | x ∈ V}` and defines local boundedness at a point, local boundedness everywhere, and boundedness of a mapping. The remarks the book records immediately afterwards are proved: `S(x̄)` is bounded, a bounded mapping is locally bounded, the equivalent explicit form `S(IB(x̄, δ)) ⊂ IB(0, ρ)` for some `δ, ρ > 0`, and vacuous local boundedness at every point outside `cl(dom S)`. |
| 5.15 Boundedness of images | **Exact** | [`LocalBoundedness.lean`](RockafellarWets/Chapter5/LocalBoundedness.lean) proves that local boundedness is exactly the property of carrying bounded sets to bounded sets, by covering the compact set `cl B` with finitely many neighborhoods of bounded image, and proves the sequential version stated alongside it. The set-image equivalence needs no normed structure on the target; only the sequential form does. |
| 5.16 Local boundedness of inverses | **Exact** | [`LocalBoundedness.lean`](RockafellarWets/Chapter5/LocalBoundedness.lean) proves that `S⁻¹` is locally bounded exactly when `‖xν‖ → ∞` with `uν ∈ S(xν)` forces `‖uν‖ → ∞`, via 5.15 applied to `S⁻¹` together with the extraction of a norm-divergent sequence from an unbounded set. |
| 5.17 Level boundedness as local boundedness | **Exact** | [`LevelBoundedness.lean`](RockafellarWets/Chapter5/LevelBoundedness.lean) proves (a) and (b). Clause (a) needs none of the covering machinery of 5.15: necessity is that local boundedness at `α` already bounds the single value `lev≤α f`, and sufficiency uses only the nesting of level sets, so that the unit ball of levels around `α` has image inside `lev≤(α+1) f`. It is stated for the explicit mapping and again through the profile mapping of 5.5, where it is `Ef⁻¹`. Clause (b) is stated against Definition 1.16, which exists in this project as `IsLevelBoundedInXLocallyUniformly` in [`Chapter3/Parametric.lean`](RockafellarWets/Chapter3/Parametric.lean), where it was introduced for parametric minimization. That definition measures boundedness of the sublevel set inside the product space, cut down by a closed ball of parameters, while 5.17(b) measures it in `IRⁿ`; the two are reconciled by `isBounded_inter_levelSet_iff`, which says a subset of a product with bounded second projection is bounded exactly when its first projection is. The final assertion, that the joint mapping `(u, α) → {x | f(x,u) ≤ α}` is locally bounded too, follows since one level serves a whole neighborhood of levels. |
| 5.18 Horizon criterion for local boundedness | **Exact** | [`HorizonMappings.lean`](RockafellarWets/Chapter5/HorizonMappings.lean) defines the horizon mapping of formula 5(5) fibrewise from the Chapter 3 horizon cone of the graph, so that `gph S∞ = (gph S)∞` holds by `rfl`, and proves all three assertions. The first replaces the book's cluster-point extraction by rescaling the values of an unbounded image by `max ‖uν‖ 1`, which never vanishes and still diverges, so the rescaled values lie in the unit ball and a convergent subsequence exists; the limit is a unit vector in `S∞(0)`. The second is free from idempotence: `(gph S)∞` is a closed cone, hence its own horizon cone, so `(S∞)∞ = S∞`. The third uses only the ray half of 3.6 and needs no finite-dimensionality, which is recorded by omitting those instances. The remarks the book records alongside 5(5) are proved as well -- `S∞` is osc, `0 ∈ S∞(0)`, `S∞` is positively homogeneous, and graph-convexity passes to `S∞` -- but the sequential description 5(6) of `S∞(x)` is not formalized, since nothing in 5.18 uses it. The remaining remark `(S⁻¹)∞ = (S∞)⁻¹` is proved in [`HorizonImages.lean`](RockafellarWets/Chapter5/HorizonImages.lean), where 5.26 first needs it. |
| 5.19 Outer semicontinuity under local boundedness | **Exact** | [`LocalBoundedness.lean`](RockafellarWets/Chapter5/LocalBoundedness.lean) proves the equivalence, with the two halves stated separately because only one of them uses the hypothesis. Sufficiency needs neither local boundedness nor properness of the target: a point of the outer limit outside the closed set `S(x̄)` is separated from it by a ball whose complement is an admissible open set `O`. Necessity is the half that uses local boundedness, and is done filter-natively rather than by the book's cluster-point argument: values outside `O` are met frequently, local boundedness confines them to a bounded set, and the compact extraction `outerSetLimitAlong_inter_nonempty_of_frequently` of [`SetLimitsAlong.lean`](RockafellarWets/Chapter5/SetLimitsAlong.lean) produces a point of the outer limit inside `Oᶜ`. |
| 5.20 Continuity of single-valued mappings | **Exact** | [`LocallyBoundedContinuity.lean`](RockafellarWets/Chapter5/LocallyBoundedContinuity.lean) views a single-valued mapping set-valued as `x → {F(x)}` and proves the three properties equivalent, as a `List.TFAE` and as separate lemmas. The equivalence of (a) and (c) is unfolding: inner semicontinuity of `x → {F(x)}` says exactly that every neighborhood of `F(x̄)` eventually contains `F(x)`. The substantive implication is (b) to (a), which is the neighborhood property of 5.19 applied to the open ball `IB(F(x̄), ε)`. The book's example showing that outer semicontinuity alone is not enough is formalized as well: `y → y⁻¹` is osc at `0` as a set-valued mapping but not locally bounded there, and in Lean it needs no case split, since `(0 : ℝ)⁻¹ = 0` reproduces the book's `F(0) = 0`. The paragraph between 5.20 and 5.21 is covered too -- truncations `S∩B : x → S(x) ∩ B` are defined, `S` is shown osc exactly when every truncation by a compact set is osc, and since truncations by bounded sets are locally bounded, 5.19 applies to each and gives the corrected neighborhood characterization of outer semicontinuity for mappings with unbounded values. The rotating-ray counterexample of Figure 5-7 is not formalized. |
| 5.21 Continuity of locally bounded mappings | **Exact** | [`LocallyBoundedContinuity.lean`](RockafellarWets/Chapter5/LocallyBoundedContinuity.lean) proves that at a point of local boundedness continuity is convergence of the Pompeiu--Hausdorff distance of 4.13 to `0` along the neighborhood filter. The book routes this through the sequential statement 4.40, but no sequences are needed: 5.12 already states each half of semicontinuity as an `ε`-inclusion on the ball `ρIB`, and local boundedness supplies a single `ρ` containing every nearby value, so the two clauses of 5.12 are exactly the two hypotheses of `hausdorffEDist_le_of_infEDist`. The printed hypothesis that `S(x)` be nonempty near `x̄` is shown unnecessary and dropped -- a finite Pompeiu--Hausdorff distance to a nonempty set already forces nonemptiness, and where `S(x̄)` is empty both sides fail together -- and closedness is needed only at `x̄` itself. The book's literal hypotheses are kept as a corollary. Sufficiency uses neither local boundedness nor 5.12. |
| 5.22 Optimal-set mappings | **Exact** | [`OptimalSetMappings.lean`](RockafellarWets/Chapter5/OptimalSetMappings.lean) proves all three clauses for `P = argminₓ f(x,u)` and `p = infₓ f(x,u)` over a set `U ⊂ dom p`. The book's Detail defers to Theorem 1.17, which this project does not have, so nothing is taken from it. The local-boundedness clause needs no semicontinuity: a bound `p ≤ α` near the parameter confines each optimal set inside `lev≤α f(·,u)`, and 5.17(b) makes those uniformly bounded. The outer-semicontinuity clause needs no level boundedness and only the *upper* half of the continuity of `p`: if a limit of optimal solutions were not optimal, lower semicontinuity of `f` would hold `f` above some `β` on a product neighborhood while upper semicontinuity of `p` pushed the nearby optimal values below `β`. It is proved at a point of `U` and again in the set-wide `SvOscOn` form. The third clause combines the first two with the relative form of 5.19, and needs in addition that `P(u)` be nonempty on `dom p` -- clause (a) of 1.17 -- which is derived here rather than assumed, by applying Chapter 3's existence theorem for minimizers to the slice `f(·,u)`, whose level boundedness again comes from 5.17(b). The relativization paragraph the book states just before 5.22 is formalized in [`LocallyBoundedContinuity.lean`](RockafellarWets/Chapter5/LocallyBoundedContinuity.lean): the restriction `S|X`, its identity `S|X = (S⁻¹∩X)⁻¹` as an inverse truncation, local boundedness relative to `X` as ordinary local boundedness of `S|X`, outer semicontinuity relative to `X` as outer semicontinuity of `S|X`, and the resulting relative form of 5.19. |
| 5.23 Proximal mappings and projections | **Partial** | Clause (a) is proved in [`ProjectionMappings.lean`](RockafellarWets/Chapter5/ProjectionMappings.lean); clause (b), on proximal mappings `Pλf`, is **missing**. For (a) the projection mapping is defined as the book defines it, the points of `C` nearest to `x`, and outer semicontinuity is obtained directly rather than by the specialization to 5.22 that the Detail suggests: the graph is visibly the intersection of `{p | p.2 ∈ C}` with one closed set per competitor `w' ∈ C`, so 5.7(a) applies, and going through the extended-real integrand `|w - x| + δ_C(w)` of 1.20 is unnecessary. Local boundedness is separate and needs only nonemptiness of `C`, outer semicontinuity only closedness. The nonemptiness clause of 1.20 is proved as well. The printed hypothesis in 5.23(a) is that `C` be nonempty, but 1.20, which it specializes, also requires `C` closed, and closedness is genuinely needed: `projMapping_Ioo_zero` and `not_svOsc_projMapping_Ioo` show that the projection onto `(0,1) ⊂ IR` is the identity on the interval and empty at `0`, hence not outer semicontinuous there. Clause (b) is out of reach for now: this project has no extended-real proximal mapping (Chapter 1's `proximalMapping` is defined only for real-valued `f`, without the properness and lower semicontinuity conventions), and no counterpart of 1.24 linking prox-boundedness below the threshold `λf` to level boundedness in `w` locally uniformly in `x`, which is what 5.22 would have to be applied to. |
| 5.24 Continuity of perturbed mappings | **Exact** | [`PerturbedMappings.lean`](RockafellarWets/Chapter5/PerturbedMappings.lean) defines the pointwise sum `S₀ + T` and the product mapping `x → S(x) × T(x)`, and proves the outer, inner and continuous clauses. The two halves are separated by which hypothesis they consume: inner semicontinuity of the sum follows from inner semicontinuity of the summands alone, with local boundedness playing no part, whereas outer semicontinuity uses local boundedness of `T` twice -- to make `T(x̄)` compact, so that `S₀(x̄) + T(x̄)` is closed, and to confine the second summands to a ball so that the pairs producing points near `ū` lie in a compact set for the extraction lemma. Both go through the product mapping, which keeps the summands apart. The "in particular" case `S(x) = C + F(x)` for closed `C` and continuous `F` is proved as a corollary, using the constant-mapping limits and the single-valued criteria of 5.20. |
| 5.25 Closedness of images | **Exact** | [`ClosedImages.lean`](RockafellarWets/Chapter5/ClosedImages.lean) proves both clauses in both cases, together with the parenthetical statements that `rge S` and `dom S` are closed. Clause (b) is proved and clause (a) is read off it by inversion, exactly as the book does, using `S(C) = (S⁻¹)⁻¹(C)` and 5.7(a). The compact case is 5.7(b) with `X = IRⁿ`, already available and needing no structure on the spaces. The locally bounded case follows the book's projection argument: `B ∩ S⁻¹(D)` is the first projection of `(gph S) ∩ (B × D)`, closed because all three factors are and bounded because it lies in `B × S(B)` with `S(B)` bounded by 5.15, hence compact. The final step, that a set meeting every compact set in a closed set is closed, is isolated as the reusable `isClosed_of_forall_isClosed_inter_isCompact`. |
| 5.26 Horizon criterion for a closed image | **Exact** | [`HorizonImages.lean`](RockafellarWets/Chapter5/HorizonImages.lean) proves both clauses in full, closedness and the trailing horizon inclusions, together with the two parenthetical sufficient conditions the book flags. Clause (b) is proved and clause (a) read off it by inversion, which needs the identity `(S⁻¹)∞ = (S∞)⁻¹` of the remarks after 5(5); that is proved here too, from 3.10 applied to the coordinate swap, whose kernel condition is vacuous. The book's Guide suggests deducing closedness from local boundedness of the truncation `S∩D` and then 5.25; that intermediate claim is proved, but the results take a shorter route. Since `S⁻¹(D)` is the image of `(gph S) ∩ (IRⁿ × D)` under `(x, u) → x`, Theorem 3.10 of Chapter 3 applies directly, and its kernel condition unpacks to the book's hypothesis `S∞(0) ∩ D∞ = {0}` via the inclusion of the truncation's horizon mapping in `S∞` and `D∞`. The equality clause of the same theorem, `L(C∞) = L(C)∞`, holds under the identical hypothesis, so the horizon inclusions cost nothing extra; they are recorded as needing neither outer semicontinuity of `S` nor closedness of `D`. |
| 5.27 Cosmic semicontinuity | **Adapted** | [`CosmicSemicontinuity.lean`](RockafellarWets/Chapter5/CosmicSemicontinuity.lean) proves both halves for a mapping `S(x) = C(x) ∪ dir K(x)` with `K(x)` a cone, together with the continuity form. The outer half is the printed one: `C(x̄) ⊃ lim sup C(x)` and `K(x̄) ⊃ lim sup∞ C(x) ∪ lim sup K(x)`. **The printed inner half is wrong.** The book writes `K(x̄) ⊂ lim inf∞ C(x) ∪ lim inf K(x)` and proves 5.27 by citing 4.20, but 4.20 -- in the book and in [`MixedCosmicLimits.lean`](RockafellarWets/Chapter4/MixedCosmicLimits.lean) -- delivers the *mixed* horizon inner limit `lim inf∞ (C(x) ∪ K(x))`, which is strictly larger, because an inner limit does not split over a union: a direction can be approached from the ordinary part on one side of `x̄` and from the direction part on the other. The corrected inner condition is what is proved, and `printed_inner_condition_of_527_fails` exhibits a mapping on `IR` which is cosmically inner semicontinuous at `0` while the printed condition fails there. The book says 5.27 "is obvious from 4.20"; it is not, because this project's 4.20 is proved by extracting convergent subsequences and no subsequence is available along a neighborhood filter. The whole of 4.20 is therefore reproved along an arbitrary index filter, from two neighborhood-transfer lemmas that trade the directions of a cone for faraway ordinary points of it and back; each transfer shrinks one neighborhood to another, so the outer and inner halves come out together. The supporting filter versions of the ordinary-part identification, of `lim sup (A ∪ B) = lim sup A ∪ lim sup B`, and of the identity `lim sup∞ K = lim sup K` for cones are proved as well. Figure 5-8 and the remark that its mapping is not cosmically osc are not formalized. |
| 5.28 Total continuity | **Exact** | [`TotalContinuity.lean`](RockafellarWets/Chapter5/TotalContinuity.lean) restates Definition 4.23 and Proposition 4.24 along an arbitrary index filter -- the Chapter 4 proofs transfer word for word once the ordinary part of a cosmic limit is available filter-natively -- and defines total continuity at `x̄` as total convergence along the neighborhood filter of formula 5(1). The displayed equivalent `lim S(x) = S(x̄)`, `lim∞ S(x) = S(x̄)∞` is proved, along with total outer semicontinuity and the identity of total continuity with inner semicontinuity plus total outer semicontinuity. The closing paragraph is proved too: the horizon inclusion `lim inf∞ S(x) ⊃ S(x̄)∞` that "total inner semicontinuity" would add is automatic from `lim inf S(x) ⊃ S(x̄)`, by the filter version of 4.21(c), so the notion would be no stronger than ordinary inner semicontinuity. |
| 5.29 Criteria for total continuity | **Adapted** | [`TotalContinuity.lean`](RockafellarWets/Chapter5/TotalContinuity.lean) proves the criterion -- total continuity at `x̄` is continuity at `x̄` together with `lim sup∞ S(x) ⊂ S(x̄)∞` -- as the filter form of 4.24 read through the equality form of 5.4, and proves all three automatic cases. Local boundedness is the shortest: the nearby values lie in one bounded set, whose cosmic closure acquires no direction points, so `lim sup∞ S(x) = {0}`. Cone-valuedness near `x̄` uses the identity `lim sup∞ K = lim sup K` for cones together with the fact that a closed cone is its own horizon cone. The convex case is the only one needing work, and it **requires the extra hypothesis `S(x̄) ≠ ∅`**, which 5.29 omits but Theorem 4.25, the result it cites, assumes throughout; `convex_clause_of_529_needs_nonempty` exhibits a convex-valued mapping on `IR`, continuous at `0` with `S(0) = ∅`, that is not totally continuous there. Its proof does not follow the sequential argument of 4.25(a), which extracts one subsequence of escaping points and another of points near a fixed `x₀ ∈ S(x̄)` and then diagonalizes: along a filter, escape is a `∃ᶠ` statement and nearness and convexity are `∀ᶠ` statements, so `Filter.Frequently.and_eventually` combines them with no extraction at all. |
| 5.30 Images of converging sets | **Exact** | [`ImageConvergence.lean`](RockafellarWets/Chapter5/ImageConvergence.lean) proves all four clauses, each alternative of (b) and (c) separately.  The Guide says to extend the argument for 4.26 using ideas from 5.26; both are used, but through the single identity `S(C) = π₂((gph S) ∩ π₁⁻¹(C))` -- the same projection device by which 5.26 read `S⁻¹(D)` -- rather than by repeating either proof.  It turns the set-valued image into an ordinary continuous image of a varying set, so 4.26 applies verbatim with `G = π₂`, and clause (b) reduces to two monotonicity inclusions for the graph slices plus 4.26's escape condition.  Outer semicontinuity is consumed only as `lim sup (gph S) = gph S`.  Of the two alternatives the book offers for that escape condition, local boundedness of `S⁻¹` needs no extraction at all, being 5.15 applied to `S⁻¹`; the horizon alternative is where the subsequence is taken, and rescaling the graph points to kill the values turns an escaping argument direction into a point of `(S∞)⁻¹(0) ∩ lim sup∞ Cν`.  Clause (a) is unrelated to all of this, since inner limits do not survive the intersection with `gph S`; it is proved directly, needs no structure on the spaces, and is stated in the sharper form requiring inner semicontinuity only at the points of `lim inf Cν`.  Clause (c) is (a) and (b) together, the limit being squeezed between the inner and outer limits, with the horizon hypothesis of (b) obtained from that of (c) by 4.24.  Clause (d) is 4.24 applied to (c), the remaining horizon inclusion `lim sup∞ S(Cν) ⊂ S∞(C∞) ⊂ S(C)∞` being proved by the double rescaling of 4.27.  That argument uses **no semicontinuity of `S` whatsoever**, so the printed hypothesis of total continuity is stronger than needed: (d) is stated under continuity, exactly as in (c), and the printed form is kept as a corollary.  Definition 5.28 is given its set-wide form `SvTotallyContinuous` in [`TotalContinuity.lean`](RockafellarWets/Chapter5/TotalContinuity.lean) for that corollary. |
| 5.31 Pointwise limits of mappings | **Exact** | [`GraphicalLimits.lean`](RockafellarWets/Chapter5/GraphicalLimits.lean) defines `pointwiseOuterLimit`, `pointwiseInnerLimit` and `PointwiseConverges` as the Chapter 4 sequential limits taken one point at a time, proves the inner-outer inclusion, the two-sided form of pointwise convergence, and the book's ordering of mappings `S₁ ⊂ S₂ ⟺ gph S₁ ⊂ gph S₂`.  Section E leaves the neighborhood filters of Sections A--D behind, so this and the following files work with `atTop` throughout. |
| 5.32 Graphical limits of mappings | **Exact** | [`GraphicalLimits.lean`](RockafellarWets/Chapter5/GraphicalLimits.lean) defines `graphicalOuterLimit`, `graphicalInnerLimit` and `GraphicalConverges` fibrewise, so that `gph (g-lim sup Sν) = lim sup gph Sν` holds by `ext` and `rfl`, and proves the displayed value formulas of the definition, the two-sided form of graphical convergence, and every remark the book records alongside: both graphical limits are osc, hence so is a graphical limit when it exists; the graphical inner limit of graph-convex mappings is graph-convex by 4.15 and is then isc on the interior of its domain by 5.9(b); the inversion identities 5(10) for the outer limit, the inner limit and graphical convergence; and the inclusions 5(11) relating pointwise to graphical limits, with the consequence `p-lim Sν ⊂ g-lim Sν`.  The displayed formula for the inner limit needs a selection lemma proved here, `mem_innerSetLimit_iff_exists_seq`: unlike the outer limit, which needs a subsequence, inner-limit membership is witnessed by a single sequence, since 4.2 makes the extended distance to the sets tend to `0` and a point within `1/(n+1)` of that distance can be chosen in each set.  Figure 5-9, where the pointwise and graphical limits both exist and differ, is not formalized. |
| 5.33 Graphical limit formulas at a point | **Exact** | [`GraphicalLimitFormulas.lean`](RockafellarWets/Chapter5/GraphicalLimitFormulas.lean) proves all four formulas, together with 5(7) and 5(9).  The `limδ↓0` formulas are rearrangements of quantifiers: the product ball `IB(x, δ) × IB(u, ε)` meets `gph Sν` exactly when `IB(u, ε)` meets `Sν(x + δIB)`, and since the sets shrink with `δ` the limit is the intersection over `δ > 0`.  The union-over-sequences formulas are where the two limits part company.  For the inner limit the witnessing sequence is the one the selection lemma of 5.32 already supplies; for the outer limit only a subsequence is available, and it is spread out to a full sequence with `Function.extend`, parked at `x` off the subsequence -- which costs nothing, since only frequent hits are claimed.  Formula 5(7) is recorded both pointwise, through the definition `GraphicalConvergesAt` that the book introduces just afterwards, and for the whole sequence.  Formula 5(9) is proved in the filter-native form `g-lim sup Sν(x̄) = lim sup_{(ν,x) → (∞,x̄)} Sν(x)`, the outer limit along the product filter `atTop ×ˢ 𝓝 x̄` of [`SetLimitsAlong.lean`](RockafellarWets/Chapter5/SetLimitsAlong.lean); the book's remark that the inner limit admits no such bivariate description is recorded but its counterexample is not formalized.  Graphical convergence relative to a set `X`, defined in the paragraph after 5.33, is formalized in [`RelativeGraphicalConvergence.lean`](RockafellarWets/Chapter5/RelativeGraphicalConvergence.lean): `graphicalOuterLimitWithin` and `graphicalInnerLimitWithin` restrict the unions above to sequences lying in `X`, `GraphicalConvergesWithinAt` and `GraphicalConvergesOn` are 5(7) with `xν` so constrained, and the book's remark that for closed `X` this is graphical convergence of the restrictions `Sν|X → S|X` is proved.  The identification splits at `X` and uses closedness only off it: at a point of `X` a witnessing sequence for the restrictions already lies in `X` where it matters -- eventually for the inner limit, frequently for the outer one -- and the stray indices are repaired by `x̄ ∈ X` itself, whereas off `X` the restrictions are empty-valued and what must vanish is their graphical outer limit, which is exactly `cl X = X`.  Closedness is necessary: `restrict_not_graphicalConverges_of_not_isClosed` is a constant sequence converging graphically relative to `X = (0, ∞)` whose restrictions do not converge graphically, their graphs reaching a point over `0`. |
| 5.34 Uniformity in graphical convergence | **Adapted** | [`GraphicalUniformity.lean`](RockafellarWets/Chapter5/GraphicalUniformity.lean) proves both clauses.  Clause (a) is 4.10 for the graphs read through the sup norm on `IRⁿ × IRᵐ`: under that norm a product ball is a pair of balls, so `(x, u) ∈ gph S ∩ ρIB` says `u ∈ S(x) ∩ ρIB` with `|x| ≤ ρ`, and `(x, u) ∈ gph T + εIB` says `u ∈ T(IB(x, ε)) + εIB`, which turns the two displayed inclusions into 4.10's `EventuallyInnerApproximates` and `EventuallyOuterApproximates` verbatim.  **The printed hypothesis that `S` be closed-valued is not enough**: `closedValued_not_enough_in_534` exhibits a closed-valued mapping on `IR` for which the constant sequence `Sν ≡ S` satisfies both inclusions for every `ε` and `ρ` -- as any constant sequence does, since `x ∈ IB(x, ε)` -- yet does not converge graphically to `S`, because `gph S` is not closed and graphical limits always are.  What 4.10 needs, and what is assumed here, is outer semicontinuity of `S`; the necessity half needs no hypothesis at all.  Clause (b) follows the Guide, using 5(9) to turn an escaping selection into an ordinary sequence of connected sets whose outer limit sits inside the bounded set `g-lim sup Sν(x̄)`, so that 4.12 confines them to one bounded set and contradicts their divergence; the conclusion for the graphical outer limit itself is then read off 5(9) again.  The book's parenthetical that the distance hypothesis "is certainly satisfied when `S = g-limν Sν` and `S(x̄) ≠ ∅`" is **also false** under the convention `d(u, ∅) = ∞` that Chapter 5 uses throughout (see 5.11): one empty value arbitrarily close to `x̄` already makes the `lim sup` infinite, and `graphicalLimit_nonempty_not_enough_in_534b` exhibits a mapping that is its own graphical limit with `S(0)` nonempty and bounded for which the hypothesis fails.  The Guide's remark that `ρIB` and `εIB` could be replaced by arbitrary bounded sets and neighborhoods of the origin is not formalized. |
| 5.35 Graphical convergence of projection mappings | **Exact** | [`ProjectionGraphicalConvergence.lean`](RockafellarWets/Chapter5/ProjectionGraphicalConvergence.lean) proves the equivalence.  The converse half is the cheap one: a point of a set is its own nearest point, so `(x, x) ∈ gph P_C` whenever `x ∈ C`, and both set-limit inclusions for `Cν` are read off the corresponding inclusions for the graphs -- no closedness, no nonemptiness, no strict convexity.  The forward half splits.  Its outer inclusion is a limit of the competitor inequality `|wν - xν| ≤ |z - xν|`, the competitors `zν ∈ Cν` converging to a given `z ∈ C` being supplied by the selection lemma of 5.32.  Its inner inclusion is the book's argument through the point `xε = x + ε(w - x)`, where the projection collapses to `{w}`; the nearby projections are then bounded and every cluster point of them is a projection onto `C` at `xε`, hence `w`.  **That collapse needs the norm to be strictly convex**, which the book's Euclidean `IRⁿ` supplies for free but a general finite-dimensional normed space does not: with the sup norm on `IR²` and `C = {1} × [0,1]`, every point of `C` is nearest to the origin and stays nearest all the way in, so `P_C(xε)` is not a singleton and the whole segment fails to be reachable in the inner limit.  `StrictConvexSpace ℝ E` is therefore assumed, exactly as Chapter 4 assumes it for the convex clause of 4.9; the counterexample is described but not formalized.  The book's Detail dismisses `C = ∅` separately and then says "we may as well assume `Cν ≠ ∅` for all `ν`"; neither step is taken here, since the nonemptiness the inner half needs is produced from a point of `C` at the moment it is needed, so the theorem holds for arbitrary closed `Cν` and arbitrary `C` in one piece.  Formula 5(12) is not stated separately, its two halves being the outer inclusion above and the distance bound derived inside the inner half. |
| 5.36 Extraction of graphically convergent subsequences | **Exact** | [`GraphicalCompactness.lean`](RockafellarWets/Chapter5/GraphicalCompactness.lean) defines escape to the horizon for a sequence of mappings, exactly as the book states it just before 5.36, and identifies it with the vanishing of the outer limit of the graphs, equivalently with the graphical outer limit being empty-valued.  That identification is where the sup norm on `IRⁿ × IRᵐ` does the work: a product of balls is a ball, so quantifying over pairs of bounded sets is the same as quantifying over balls about the origin of the product, which is the form Corollary 4.11 uses.  The theorem itself is then 4.18 for the sequence of graphs, the limit mapping being reconstructed from its graph by `svOfGraph`. |
| 5.37 Approximation of generalized equations | **Exact** | [`GeneralizedEquations.lean`](RockafellarWets/Chapter5/GeneralizedEquations.lean) proves all three clauses.  Both are direct: (a) unfolds the definition of the graphical outer limit, so that a cluster point of approximate solutions is a true solution; (b) is the sequential description of the graphical inner limit from 5.32, read through the inverse, and is recorded both in the printed form `S⁻¹(ū) ⊂ ⋂_{ε>0} lim inf (Sν)⁻¹(IB(ū, ε))` and in the form the book paraphrases it by, an explicit sequence of approximate solutions with right-hand sides converging to `ū`.  The printed hypothesis that `S` and the `Sν` be closed-valued is used by neither clause and is dropped. |
| 5.38 Equicontinuity properties | **Exact** | [`Equicontinuity.lean`](RockafellarWets/Chapter5/Equicontinuity.lean) defines equi-osc, equi-isc and equicontinuity at a point relative to a set, together with their asymptotic versions, the book's index set `N ∈ N∞` being `∀ᶠ ν in atTop`.  The remarks the book records afterwards are proved: equicontinuity implies asymptotic equicontinuity; equi-outer semicontinuity forces each term to be outer semicontinuous and equi-inner semicontinuity forces each to be inner semicontinuous, so an equicontinuous sequence consists of continuous mappings; and the book's example `Sν(x) = {1/ν}, [-1/ν, 1/ν], {-1/ν}` for `x > 0`, `x = 0`, `x < 0` is formalized and shown to be asymptotically equicontinuous at `0` while no term of it is inner semicontinuous there, so asymptotic equicontinuity is strictly weaker. |
| 5.39 Equicontinuity of single-valued mappings | **Exact** | [`Equicontinuity.lean`](RockafellarWets/Chapter5/Equicontinuity.lean) proves the equivalence and its asymptotic counterpart, together with the explicit `ε`-`ρ` form of both conditions and the final remark that `ρ` may be dropped for an eventually locally bounded sequence.  The substance is one geometric step.  Equi-outer semicontinuity controls `Fν(y) - Fν(x̄)` only where `|Fν(y)| ≤ ρ`, while equi-inner semicontinuity asks for that control where `|Fν(x̄)| ≤ ρ`; continuity of each `Fν` bridges the two by the intermediate value theorem, since a value leaving the ball of radius `ρ + ε` along the segment from `x̄` to `y` would first have to cross that sphere, and at the crossing point equi-outer semicontinuity already confines it within `ε` of `Fν(x̄)`.  The printed "Moreover" clause is stated with `N ∈ N∞`, which belongs to the asymptotic notion; both versions are proved, so nothing is lost either way. |
| 5.40 Graphical versus pointwise convergence | **Exact** | [`GraphicalPointwise.lean`](RockafellarWets/Chapter5/GraphicalPointwise.lean) proves the two displayed equalities and the three-way statement.  By 5(11) the pointwise limits always sit inside the graphical ones, so only the reverse inclusions are at issue, and both come from the same computation: a value `uν ∈ Sν(xν)` with `xν → x̄` is trapped in a fixed ball, equi-outer semicontinuity moves it within `ε` of `Sν(x̄)`, and `uν → ū` carries `ū` there too -- the outer statement differing from the inner one only in reading `∃ᶠ` for `∀ᶠ`.  With (a) in hand, (b) and (c) become the same condition; the remaining implication (b) + (c) ⇒ (a) is the book's contradiction argument and is the only place where compactness of bounded sets in the target is used.  The printed closed-valuedness hypothesis is nowhere needed.  Everything is proved relative to a set `X`, since that is the form 5.44 needs, the printed statement being the case `X = IRⁿ`; the relative proofs are the absolute ones read through the restrictions `Sν|X` of 5.33, and the extra hypothesis `x̄ ∈ X` is used only for the easy inclusions, where the constant sequence at `x̄` has to be admissible. |
| 5.41 Continuous and uniform limits of mappings | **Exact** | [`ContinuousUniformConvergence.lean`](RockafellarWets/Chapter5/ContinuousUniformConvergence.lean) defines continuous convergence at a point, relative to a set and everywhere, and uniform convergence on a set, and proves both identifications the book records immediately afterwards.  Both are 4.10 in disguise: pointwise convergence at a single point is the pair of `ε`-inclusions between `Sν(x̄)` and `S(x̄)` on every bounded ball, and continuous convergence at `x̄` is the same pair of inclusions between `Sν(x)` and `S(x̄)`, holding uniformly for `x` in some neighborhood of `x̄` -- proved relative to a set `X` and specialized.  The awkward half, that the sequential definition implies the neighborhood form, spreads a sequence of bad indices out into a single approaching sequence with `Function.extend`, as in 5.33; here the bad indices need not be injective, so the redistributed sequence lands on *some* bad argument for the same bad index, which is all the contradiction needs.  The remark that `dlρ` and `dl̂ρ` of 4(11) give further descriptions is not formalized. |
| 5.42 Distance function descriptions of convergence | **Exact** | Clause (a) is in [`ContinuousUniformConvergence.lean`](RockafellarWets/Chapter5/ContinuousUniformConvergence.lean), being 4.7 verbatim; clauses (b) and (c) are in [`DistanceDescriptions.lean`](RockafellarWets/Chapter5/DistanceDescriptions.lean).  Distances are extended-real throughout Chapter 5 (see 5.11), so (a) and (b) need no truncation, the empty value being handled by `d(u, ∅) = ∞`.  Clause (b) rests on `eventually_inclusions_iff_tendsto_infEDist`, which fuses 4.7 and 4.10 and states them along an *arbitrary* filter against one **fixed** closed target; the point is that the `ε`-`ρ` form of continuous convergence recorded after 5.41 is already a statement along the product filter, `∃ V ∈ N(x̄), ∀ᶠ ν, ∀ y ∈ V ∩ X` and `∀ᶠ (ν, y) in atTop ×ˢ N_X(x̄)` being the same thing unfolded, so (b) is exactly that hub instantiated at `atTop ×ˢ N_X(x̄)`.  No subsequence is extracted anywhere; the only compactness used is two finite subcovers, of `C ∩ ρIB` and of `ρIB \ (C + εIB)`, in the direction that builds inclusions out of distances -- the same device as in 5.12.  Taking `l = atTop` recovers 4.7 and 4.10 together, and taking `X = IRⁿ` gives the printed absolute form of (b) alongside the relative form the book records just afterwards.  Clause (c) does not fit that hub, its target `S(x)` moving with `x`, and is proved directly: both directions turn on a single finite cover of the compact *ball* `ρIB` -- not of any set built from `S` -- which is what makes the estimate uniform over `x ∈ X`, and the radius `‖u‖ + η + 1` is likewise chosen once for all `x`.  The truncation `truncInfDist η u C = min(d(u, C), η)` is what lets the statement survive empty values under a real-valued uniform limit. |
| 5.43 Continuous versus uniform convergence | **Adapted** | [`ContinuousVersusUniform.lean`](RockafellarWets/Chapter5/ContinuousVersusUniform.lean) proves the equivalence and the general assertion recorded after it.  The book translates the whole argument, through 5.42, into scalar functions `hν(x) = min(d(u, Sν(x)), η)`; that route is not taken.  Every notion involved already has an `ε`-`ρ` *inclusion* form here -- uniform convergence is Definition 5.41 itself, continuous convergence at a point is `svConvergesContinuouslyWithinAt_iff`, and continuity of `S` relative to `X` is 5.12 -- so the theorem is the composition of two inclusions through a third set, each composition costing `ε/2` on each side and `ε/2` of extra radius in the middle, which is why every statement is applied at radius `ρ + ε` before being used at radius `ρ`.  **Nothing here needs 5.42.**  The one step that is not pure composition is uniform convergence on a compact `B ⊂ X`: continuous convergence supplies a neighborhood at each point of `B` separately, and a finite subcover turns those into a single index good at every point of `B` at once, replacing the book's contradiction argument through a cluster point of escaping arguments.  **The last sentence of (b) is false as printed.**  That the continuity of `S` is automatic from uniform convergence when the `Sν` are continuous relative to `X` and `X` is locally compact relative to itself needs `S` closed-valued on `X` -- the standing assumption of 5.42 that 5.43 does not restate.  The reason is structural: the `ε`-inclusions of Definition 5.41 cannot distinguish a set from its closure, so no argument from them alone can produce closed values.  `svOscOn_not_automatic_without_closedValued` is the witness, the constant sequence `Sν ≡ [0, 1]` on `IR` -- continuous, converging uniformly on all of `IR` -- against the constant `S ≡ (0, 1)`, which is not outer semicontinuous anywhere.  The clause is proved with closed-valuedness added, as a threefold composition interposing `Sν(z)` and `Sν(x̄)` between `S(z)` and `S(x̄)`. |
| 5.44 Graphical versus continuous convergence | **Exact** | [`GraphicalContinuousConvergence.lean`](RockafellarWets/Chapter5/GraphicalContinuousConvergence.lean) proves the equivalence at a point `x̄ ∈ X`, over the relative graphical convergence of 5.33 and the relative form of 5.40.  Forward, both clauses of equicontinuity are the same composition run in opposite directions: continuous convergence approximates `S(x̄)` by `Sν(y)` from both sides at once, so routing through `S(x̄)` relates `Sν(y)` to `Sν(x̄)`.  That replaces the book's contradiction argument, which extracts a cluster point of escaping values, and the composition lemma `inter_closedBall_subset_thickening_trans` it isolates is what 5.43 and 5.46 also run on.  Backward is the book's own argument: asymptotic equi-osc plus relative graphical convergence give pointwise convergence at `x̄` by 5.40, so `ū ∈ S(x̄)` is a limit of values `uν ∈ Sν(x̄)`; those stay in a fixed ball, and asymptotic equi-isc carries them into `Sν(xν) + (ε/2)IB` for any approaching sequence in `X`.  The forward direction uses 4.10 and so is stated in finite dimensions; the backward direction needs nothing beyond a normed target. |
| 5.45 Graphical convergence of single-valued mappings | **Exact** | [`GraphicalContinuousConvergence.lean`](RockafellarWets/Chapter5/GraphicalContinuousConvergence.lean) proves the equivalence.  Eventual local boundedness is *not* a consequence of the equicontinuity 5.44 supplies -- for singleton values the equi-osc inclusion is vacuous outside the ball `ρIB` and bounds nothing -- so it is derived from continuous convergence directly, by the `Function.extend` redistribution of 5.41 (made available as `tendsto_extend_index`).  The bound chosen for the contradiction is the *fixed* `‖F(x̄)‖ + 1` rather than an unbounded one, which is what lets the bad property survive being reattached to a different `k` with the same index.  The converse is the book's: local boundedness traps every `Fν(xν)` with `xν → x̄` in a fixed ball, and a subsequence staying `ε` away from `F(x̄)` would have a cluster point at that distance lying in the graphical outer limit, which single-valuedness of the limit pins to `F(x̄)`.  Neither direction needs finite dimensionality; the converse needs a proper target. |
| 5.46 Graphical convergence from uniform convergence | **Adapted** | [`GraphicalFromUniform.lean`](RockafellarWets/Chapter5/GraphicalFromUniform.lean) proves both clauses.  Clause (a) is proved directly in the `ε`-`ρ` inclusions rather than through distances: outer semicontinuity passes to the limit by the threefold composition of 5.43 -- `S(z)` reaches `Sν(z)` by uniform convergence, `Sν(z)` reaches `Sν(x̄)` because `Sν` is osc, `Sν(x̄)` reaches `S(x̄)` by uniform convergence again, one index serving all three -- and the two halves of relative graphical convergence then come cheaply, the outer half composing uniform convergence with that outer semicontinuity and the inner half being the second inclusion of Definition 5.41 read at the single point `x̄`, which already places `S(x̄)` in the pointwise inner limit.  Clause (b) is 5.43 followed by 5.44, as the book says.  **Both clauses need the limit to be closed-valued on `X`, which the printed statement does not assume.**  The book's own proof of (a) uses it at the last step, concluding `ū ∈ S(x̄)` from `d(ū, S(x̄)) ≤ 2ε` for every `ε > 0`; the same constant witness `svOscOn_not_automatic_without_closedValued` recorded at 5.43 refutes both clauses without it, `Sν ≡ [0, 1]` being osc and continuous and converging uniformly on `IR` to `S ≡ (0, 1)`, which is neither osc nor a graphical limit of the constant graphs.  Both clauses are proved with closed-valuedness added.  The book's parenthetical that local compactness relative to `X` holds when `X` is closed or open is not formalized; the hypothesis is carried as stated. |
| 5.47 Arzelà-Ascoli, set-valued version | **Exact** | [`ArzelaAscoli.lean`](RockafellarWets/Chapter5/ArzelaAscoli.lean) assembles the theorem exactly as the Guide says -- the compactness of 5.36, then 5.44, then 5.43.  The one point needing care is that 5.36 produces *absolute* graphical convergence while 5.44 consumes convergence *relative to* `X`, and neither notion implies the other, so 5.36 is applied not to `Sν` but to the restrictions `Sν|X` of 5.33: at a point of `X` the graphical limits of the restrictions are the relative graphical limits, an identification that needs nothing of `X`, since the closedness in the paragraph after 5.33 is used only off `X` and the conclusion never looks there.  `X` is therefore arbitrary, as in the book.  Escape to the horizon is not excluded but absorbed: a sequence of graphs with empty outer limit converges, as a whole sequence, to the empty-valued mapping, so `exists_graphicalConverges_subsequence` (added to [`GraphicalCompactness.lean`](RockafellarWets/Chapter5/GraphicalCompactness.lean)) extracts a graphically convergent subsequence unconditionally.  The limit may consequently be empty-valued, which is correct: `Sν(x) = {ν}` is asymptotically equicontinuous relative to `IR` and has no other limit.  Asymptotic equicontinuity passes to subsequences because its index condition is eventual. |
| 5.48 Uniform convergence of monotone sequences | **Exact** | [`MonotoneUniformConvergence.lean`](RockafellarWets/Chapter5/MonotoneUniformConvergence.lean) proves both clauses.  The reduction to 5.43 is the book's; the remaining claim, continuous convergence relative to `X`, is proved on the `ε`-`ρ` inclusions rather than through the distance functions of 5.42(b), and is shorter that way because in each case one of the two inclusions is free.  Increasing: `Sν(y) ⊂ S(y)`, so the first inclusion is just the outer semicontinuity of `S` at `x̄`, with no index condition, while the second is the book's argument -- one index `ν₀` at which `S(x̄)` is already `ε/2`-approximated by `Sν₀(x̄)`, spread to nearby `y ∈ X` by the inner semicontinuity of that single `Sν₀`, then carried up to all `ν ≥ ν₀` by monotonicity.  Decreasing is the mirror image.  The book's "no loss of generality in assuming `Sν(x)` to be closed too" is discharged, not assumed: in the decreasing case the `Sν` are osc and so already closed-valued, and in the increasing case `SvIscWithinAt.closure` -- inner semicontinuity survives closing the values, inner limits being closed -- together with `thickening δ (cl A) = thickening δ A` makes the closure invisible in the conclusion.  The printed hypothesis `X ⊂ dom S` is not needed and is not assumed. |
| 5.49 Metric version of continuous and uniform convergence | **Adapted** | [`MetricConvergence.lean`](RockafellarWets/Chapter5/MetricConvergence.lean) proves clause (a) as printed and clause (b) with one hypothesis added.  Everything rests on a single sandwich around the integrated distance `dl` of 4(12), and the whole content of the proposition is that the sandwich is *asymmetric*.  Downward, 4.41(a) gives `e^{-ρ} dl_ρ ≤ dl`, so a small `dl` already forces the `ε`-inclusions of Definition 5.41 on `ρIB`, the tolerance degrading only by the harmless factor `e^{-ρ}` and neither set needing to be closed.  Upward, 4.41(b) bounds `dl` by `dl_ρ` plus the tail `e^{-ρ}(β + ρ + 1)`, where `β` bounds the two distances from the origin, and the position of the sets is paid for twice: once in the radius `2ρ + β` at which the inclusions must be checked, through 4.37(a), and once in the tail.  In (a) that costs nothing, because the target `S(x̄)` is fixed and continuous convergence bounds `d(0, Sν(x))` near `x̄` by itself -- one application of the inclusions at radius `d(0, S(x̄)) + 1` does it, which is `SvConvergesContinuouslyAt.exists_bound_infDist_zero`.  **Clause (b) is false as printed.**  There the target `S(x)` moves with `x` and nothing bounds `d(0, S(x))` over `X`; `svConvergesUniformlyOn_not_integratedSetDistance` is the witness, `S(x) = {2x}` on `X = [1, ∞)` approximated by `Sν(x) = {x}` for `x ≥ ν` and by `S(x)` before that, which converges uniformly on all of `X` because the discrepancy is pushed out of every fixed ball, while at `x = ν` the two values sit at distances `ν` and `2ν` from the origin, so `dl ≥ ν` by 4.41(c).  The direction that fails is exactly the one the book proves by fixing `x` and contradicting a *pointwise* statement, its `β = d(0, S(x))` being a bound at that one point only.  Clause (b) is stated with `d(0, S(·))` bounded on `X`, which is what that `β` silently assumes and which `forall_infDist_zero_le_of_forall_norm_le` supplies from bounded values; the converse direction is proved with no hypothesis beyond nonemptiness.  Both clauses are stated on nonempty closed-valued mappings, `dl` being a metric only on `cl-sets≠∅`. |
| 5.50 Quantification of graphical convergence | **Exact** | [`GraphMetric.lean`](RockafellarWets/Chapter5/GraphMetric.lean) proves every clause.  Because graphical convergence is *defined* here as Painleve--Kuratowski convergence of the graphs, the passage to the space `osc-maps≢∅` is not a translation but an identification: `OscMapModel.isometryEquivSetMetricModel` is an isometric equivalence between it and Chapter 4's hyperspace `cl-sets≠∅(IRⁿ × IRᵐ)`, an outer semicontinuous mapping with nonempty domain being exactly a nonempty closed subset of the product by 5.7, with `svOfGraph` as the inverse.  Everything the theorem asserts then transports along it: the metric clause and `graphicalConverges_iff_tendsto_dist` from 4.42, the pseudo-metric `dlρ` and the two threshold characterizations `graphicalConverges_iff_tendsto_rhoGraphDistance_from` and its `dl̂ρ` counterpart from 4.36 -- stated for an arbitrary fixed threshold `r₀`, which gives both directions of the book's `∃ ρ̄` phrasing -- and properness from 4.43, whence local compactness, completeness and, via second countability, the separability that 4.45 underwrites.  The one clause that is not transport is the parenthetical that `dl̂ρ` is not a pseudo-metric.  Chapter 4's witness lives in `IR`, and it is carried into graphs by the mappings supported at a single point of the domain, `x ↦ if x = 0 then A else ∅`, whose graph is `{0} × A`; since `a ↦ (0, a)` is an isometry of `IR` into `IR × IR` for the product norm, `rhoHatDistance_prod_singleton` shows the localized distances are unchanged, so the same three sets refute the triangle inequality here.  The closing remark is formalized too: `rhoDistance_closure` and `integratedSetDistance_closure` record that the distances see only the closures of their arguments, and `exists_ne_integratedSetDistance_svGraph_eq_zero` -- the constant mappings `(0, 1)` and `[0, 1]` on `IR` -- shows that on the larger space of all mappings `dl` is only a pseudo-metric.  The book's remark that the estimates of 4.37--4.45 all carry over is realized by the isometric equivalence rather than restated result by result. |
| 5.51 Addition of set-valued mappings | **Exact** | [`MappingOperations.lean`](RockafellarWets/Chapter5/MappingOperations.lean) proves all three clauses with their parentheticals.  Clause (a) is the pointwise inclusion `(S₁ + S₂)(V) ⊂ S₁(V) + S₂(V)` against boundedness of a Minkowski sum, and needs no covering argument.  Clause (b) runs on the device the book supplies and that 5.52(b) reuses: the sum is the *domain* of the auxiliary mapping `R(x, y) = S₁(x) ∩ (y - S₂(x))`, and 5.25 turns local boundedness of `R` into closedness of `dom R = gph(S₁ + S₂)`.  Where the book describes `gph R` by an explicit parameterization, here it is read as an intersection of two *preimages* of `gph S₁` and `gph S₂` under continuous maps of the ambient product, which is shorter and needs no closedness of images; both parentheticals follow from `R(x, y) ⊂ S₁(x)` and `R(x, y) ⊂ y - S₂(x)`.  Clause (c) is 4.29(d) in two-term form -- added to Chapter 4 as `TotalConverges.add_of_horizon_prod` -- read along an approaching sequence through `svTotallyContinuousAt_iff_forall_seq`, the sequential form of Definition 5.28 established in the same file.  Both of the book's sufficient conditions for its hypotheses are proved: convex nonempty values give the product-horizon identity by 3.11, and local boundedness on either side gives both hypotheses at once, the second because `-{0} = {0}` meets every horizon cone exactly at the origin. |
| 5.52 Composition of set-valued mappings | **Exact** | [`MappingOperations.lean`](RockafellarWets/Chapter5/MappingOperations.lean) proves all five clauses.  (a) is 5.15 twice, over `(T ∘ S)(V) = T(S(V))`.  (b) is the auxiliary-domain device of 5.51(b) again, now with `R(x, w) = S(x) ∩ T⁻¹(w)` and `dom R = gph(T ∘ S)`, both parentheticals coming from `R(x, w) ⊂ S(x)` and `R(x, w) ⊂ T⁻¹(w)`.  Clauses (c), (d) and (e) are 5.30 read along an approaching sequence, which is what `svContinuousAt_iff_forall_seq` and `svTotallyContinuousAt_iff_forall_seq` are for; the latter needs the horizon extraction `exists_seq_mem_horizonOuterSetLimit`, proved by applying the diagonal extraction of `SequentialLimits.lean` in cosmic space -- which is legitimate because `lim sup∞` along a filter is by definition `cosmicDirectionCone` of an ordinary outer limit there -- and reading the result back through `cosmicDirectionCone`.  Clause (c) is **not** obtained from (d) as the book obtains it: local boundedness of `S` bounds the argument sets directly, which gives the no-escape condition of 5.30(b) with no horizon hypothesis at all, recorded as `noConvergentImageEscapeAlong_snd_of_eventually_subset`, a third alternative to the book's two.  Both versions of (d) and the whole of (e) are then one line each. |
| 5.53 Images of converging mappings | **Adapted** | [`ConvergingImages.lean`](RockafellarWets/Chapter5/ConvergingImages.lean) defines total graph convergence and proves formula 5(14) -- which is 4.24 read through `gph S∞ = (gph S)∞`, so there is nothing to prove -- together with all three inclusions and both concluding statements.  The three inclusions are the moving-mapping counterparts of 5.30(a), (b) and (d), and each differs from its fixed-mapping original at exactly one step: where 5.30 puts a limit of graph points into the closed set `gph S`, here it goes into `lim sup gph Sν`, which graphical convergence identifies with `gph S`; and where 5.30 produces a horizon direction of `gph S` by `mem_asymptoticCone_of_seq_smul`, here it produces one of `lim sup∞ gph Sν`, which the second half of 5(14) sends into `gph S∞`.  That second step is isolated once as `mem_horizonOuterSetLimit_of_seq_smul`, the moving-set counterpart of `mem_asymptoticCone_of_seq_smul`, which normalizes the limit and so spares (b) and (c) the `cosmicDirectionOf` bookkeeping.  Clause (a) is proved from the `ε`-`ρ` form of continuous convergence rather than from its sequential definition, which avoids building an approaching sequence by diagonal choice: `Cν` meets a fixed neighborhood of `x̄` eventually, and that is exactly the quantifier the inclusions of 5.41 consume.  **The first concluding statement is false as printed.**  It asks only that `Sν` converge continuously, but the escape case of (b) needs `lim sup∞ gph Sν ⊂ gph S∞`, which continuous convergence does not supply -- as the book itself observes in the paragraph just before 5.53, with a counterexample of the same shape.  `not_pkConverges_svImage_of_svConvergesContinuously` is the witness: `Sν(x) = {0}` at the escaping argument `x = ν` and `∅` elsewhere converges continuously to the empty-valued mapping, and with `Cν = C = IR` (which converges totally, and for which the horizon hypothesis holds) every image is `{0}` while the image of the limit is empty.  Total graph convergence is therefore added to both concluding statements, and the total form of the conclusion is recorded as well. |
| 5.54 Convergence of positive hulls | **Exact** | [`PositiveHullConvergence.lean`](RockafellarWets/Chapter5/PositiveHullConvergence.lean) proves the exercise.  The Guide routes it through the mapping `S(x) = pos{x}` and the image theorems, but that mapping is continuous only *relative to* the complement of a neighborhood of the origin -- it has no continuous extension across `0`, since `cl(gph S)` contains the whole fibre `{0} × IRⁿ`, and no choice of `S(0)` repairs both semicontinuities -- whereas the images of 5.30 and 5.53 are taken under mappings continuous everywhere.  It is proved directly instead, which is also shorter.  Each hypothesis on `C` is used exactly once: compactness bounds `‖y‖` above, through eventual boundedness of the `Cν` by 4.25, and `0 ∉ C` bounds it below, through the eventual separation of the `Cν` from the origin; between them the coefficient `a = ‖u‖ / ‖y‖` stays in a compact range, which is all the extraction needs.  The inner half needs neither bound, the coefficient there being fixed in advance.  The empty limit is not excluded but handled: total convergence to `∅` forces the `Cν` to be eventually empty, and set limits are tail-determined.  The passage from ordinary to total convergence is the Guide's, by 4.25(b) on cones. |
| 5.55 Generic continuity from semicontinuity | **Exact** | [`GenericContinuity.lean`](RockafellarWets/Chapter5/GenericContinuity.lean) proves both cases, each in the form that names the missing semicontinuity and again in the book's form about continuity.  Everything is stated in the subspace `↥X`, so that the book's `intₓ`, `clₓ`, nowhere dense in `X` and meager in `X` are the ordinary `interior`, `closure`, `IsNowhereDense` and `IsMeagre` there; `preimage_val_mem_nhds_iff` and `mem_closure_preimage_val` are the translation, and 5.7(c) is restated in the subtype form `IsOpen (X ↓∩ S⁻¹(O))` that already served 5.7(b).  The book's two elementary examples of nowhere dense sets are proved as `isNowhereDense_sdiff_interior` and `isNowhereDense_closure_sdiff`, both being frontiers.  The book's countable family of rational closed balls is replaced by the balls centred in a countable dense subset of the target with radius `1/(n+1)`; the osc case uses the closed ball, where the book uses `B`, and the isc case the *concentric open* ball, where the book uses `int B`, so that `int IB(u, ρ) = B(u, ρ)` -- and with it a real vector space structure on the target -- is never needed.  A single selection lemma `exists_ball_closedBall_subset` serves both cases, and the target hypotheses are exactly those of 5.7(b), a proper pseudo-metric space, separability coming for free from properness.  Closed-valuedness is assumed only in the isc case, where it produces the ball separating `ū` from `S(x̄)`; the osc case never uses it, outer semicontinuity relative to `X` already making the values closed. |
| 5.56 Generic continuity of extended-real-valued functions | **Exact** | [`GenericContinuity.lean`](RockafellarWets/Chapter5/GenericContinuity.lean) proves both the lsc and the usc case by applying 5.55 to the epigraphical profile mapping of 5.5; the hypographical profile is not needed, since `Ef` is isc relative to `X` exactly when `f` is usc relative to `X`.  This required the 5.5 dictionary relative to a set, which [`ProfileMappings.lean`](RockafellarWets/Chapter5/ProfileMappings.lean) now supplies: its four semicontinuity equivalences are proved once along an arbitrary filter, in a form that uses no topology on the domain at all, and the absolute and the relative statements are both instances of those cores.  The closed-valuedness that the isc case of 5.55 needs is `isClosed_epiProfile`, the profile value being the preimage of `[f(x), ∞]` under the embedding of `IR` in `IR‾`. |
| 5.57 Projections as continuous selections | **Exact** | [`ContinuousSelections.lean`](RockafellarWets/Chapter5/ContinuousSelections.lean) proves the selection claim, the existence of the selections, and both readings of the concluding closure formula.  The selections are not carried as a proof-dependent `nearestPoint`: the continuity theorem is stated for an *arbitrary* function whose values are projections, and existence is proved separately, which keeps the hypotheses on `S` out of the term `sᵤ` and lets the closure formula be stated with the set-valued `projMapping` of 5.23.  The Detail's route through 2.25 and 4.9 is not the one taken.  What the argument actually needs is that `x ↦ P_{S(x)}(u)` is osc and locally bounded relative to `X`, and both come from 5.11(c) alone -- a point of the outer limit lies in `S(x̄)` because the projections do, and it realizes `d(u, S(x̄))` because the nearby projections realize the nearby distances -- after which the conclusion is 5.19 relative to `X`, already available as `SvOscWithinAt.exists_nhds_svImage_inter_subset`.  Convexity and `[StrictConvexSpace ℝ F]`, the project's standing generalization boundary, enter at one point only: to know that `P_{S(x̄)}(u)` is the single point `sᵤ(x̄)`, so that an `ε`-ball around it contains the whole image.  The closure formula needs neither, and is proved directly: a point `w ∈ S(x)` is approximated by some `u ∈ U`, whose projection is at least as close to `u` as `w` is.  The book's remark that `U = IRᵐ` needs no closure is `iUnion_projMapping`, since `u` is its own projection when `u ∈ C`.  The unnumbered remark preceding 5.57 is proved as well, by the book's own route: `d(ū, S(x)) → d(ū, S(x̄)) = 0` through 5.11(b), with the selection chosen inside `d(ū, S(x)) + |x - x̄|` of `ū`, an allowance that is positive away from `x̄` and forces the value `ū` at `x̄` itself. |
| 5.58 Michael representations of isc mappings | **Exact** | [`MichaelSelection.lean`](RockafellarWets/Chapter5/MichaelSelection.lean) proves the selection and [`MichaelRepresentation.lean`](RockafellarWets/Chapter5/MichaelRepresentation.lean) the countable representation, each first on a normal paracompact domain and then in the book's relative phrasing on a set `X`, the bridge being `svIsc_subtype_iff` in [`SemicontinuityCriteria.lean`](RockafellarWets/Chapter5/SemicontinuityCriteria.lean).  **Two of the printed hypotheses are dropped.**  σ-compactness of `dom S` is not needed: the book assumes it only in order to run its own partition-of-unity construction by hand, and Stone's theorem -- an instance in Mathlib -- already makes every metric subspace paracompact, which is exactly what that construction needs.  Finite-dimensionality is not needed either; a Banach target suffices for the selection, and separability of the target is what the *countable* representation adds.  The book's Parts 1 and 4 are Mathlib's `exists_continuous_forall_mem_convex_of_local_const`, which builds a continuous `g` with `g(y) ∈ t(y)` from a locally constant witness for a convex-valued `t`, over any normal paracompact domain; Part 2 is that lemma applied to `t(y) = S(y) + ηIB`, whose witness at `x̄` is *any* point of `S(x̄)`, and so costs no compactness at all where the book has a compact `X`.  Part 3 is the iteration, phrased with the *open* moving ball `S(y) ∩ B(sν(y), 2⁻ᵛ)`, since the slack `ρ - d(w, f(ȳ))` at an interior point is what absorbs the motion of the centre; that the refinement is again isc is proved directly in `SvIsc.inter_ball` rather than through 4.32 and 5.24 as the book does.  The successive approximations are `3·2⁻⁽ᵛ⁺¹⁾`-close uniformly in `y`, which is the hypothesis of `cauchySeq_of_le_geometric_two`, so the limit exists, is continuous because the bound does not depend on `y`, and lies *in* `S(y)` rather than in its closure because the values are closed -- the one and only use of closed-valuedness in the whole construction.  Part 5 is factored as `exists_selections_hitting_closure`, one open convex set `B` at a time: `cl(S(y) ∩ B)` is isc by the direct `SvIsc.closure_inter_isOpen` in place of the book's 4.32(c), has a selection on the open set `S⁻¹(B)`, and pasting that selection over each member of an exhaustion of `S⁻¹(B)` by closed sets turns it into a countable family of selections of `S` itself.  The exhaustion is by the inner shells `{y | 1/k ≤ d(y, S⁻¹(B)ᶜ)}` in `Metric.infEDist`, which replaces the book's unions of rational balls shrunk by `1/(2k)²` -- machinery it needs only for σ-compactness in `IRⁿ` -- and the extended distance is what makes the case `S⁻¹(B) = X`, with empty complement, need no separate treatment. |
| 5.59 Extensions of continuous selections | **Adapted** | [`MichaelRepresentation.lean`](RockafellarWets/Chapter5/MichaelRepresentation.lean) proves the extension, relative to a general set `X` and in the printed case `X = dom S`.  The whole content is the gluing lemma `svIsc_piecewise_singleton` that 5.58 already needs: `S̄(x) = {s̄(x)}` on the closed set and `S(x)` elsewhere is isc relative to `X`, since at a point of that set continuity of `s̄` handles the nearby points inside it and inner semicontinuity of `S` at `s̄(x̄) ∈ S(x̄)` handles the rest, while at a point outside it closedness gives a whole neighborhood on which the mapping is `S` itself.  No countable family is involved, so separability of the target is not needed here, and neither is σ-compactness of `dom S`.  **Closed-valuedness is added to the printed hypotheses**, which ask only for convex-valuedness.  The theorem 5.59 invokes asks for closed-convex-valuedness, and `S̄` takes the values of `S` off the prescribed set, so the book's own route cannot avoid the requirement; Michael's theorem uses closedness exactly once, to put the limit of its approximations in `S(x)` rather than in `cl S(x)`, and nothing about `S̄` recovers it.  Unlike the omissions recorded at 5.43(b) and 5.46, this one carries no counterexample: what is claimed is only that the printed hypotheses do not support the printed proof, and the missing hypothesis is carried rather than shown to be necessary. |
